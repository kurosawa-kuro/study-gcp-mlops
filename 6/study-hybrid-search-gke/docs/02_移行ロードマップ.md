# 02_移行ロードマップ (Phase 6 品質修正 + 未対応項目)

本ドキュメントは Phase 6 (`study-hybrid-search-gke`) 固有の **未対応・欠陥項目** を管理する。`docs/README.md §2` により全ドキュメントの最上位権威であり、本ドキュメントと他ドキュメントが矛盾した場合は本ドキュメントを正とする。

Phase 6 のスコープは **Phase 5 serving 層の GKE + KServe 差し替え** のみ。Vertex AI Pipelines / Feature Group / Model Registry / BigQuery / Meilisearch / 10 SA は Phase 5 から継承する (`CLAUDE.md` 非負制約表)。

---

## 0. 調査根拠 (Phase 5 との差分比較)

基準コミット: `5/study-hybrid-search-vertex/` の現行ツリー (Phase 5 は `make check` 全パス)。Phase 6 現状は `make check` で **3 tests fail / 217 pass** (`tests/unit/app/test_retrain.py`)、加えて静的検査では落ちない protocol mismatch 群が潜在。

| 深刻度 | 分類 | 内訳 |
|---|---|---|
| **CRITICAL** | serving 疎通破壊 | KServe encoder client/server protocol mismatch (6 件まとまり) |
| **CRITICAL** | CI 失敗 | `test_retrain.py` の fake clock 回帰 (3 tests) |
| **CRITICAL** | build 失敗連鎖 | `.dockerignore` の包括除外復活 (User memory 既記録の事故多発ポイント) |
| HIGH | pipeline 回帰 | `train_reranker.py` の `Output[Model]` 復活 (Phase 5 の `str` workaround を喪失) |
| HIGH | cosmetic but wrong | Container image 名の ハイフン 落ち (`property-trainer` → `trainer`) |
| MED | scripts 回帰 | `_common.py::_load_yaml` の list-block 処理と `_load_list_setting` 削除 |
| MED | 死コード | `setup_model_monitoring` は Phase 6 でスコープアウト (CLAUDE.md §5.5) なのに deploy_all step 9 に残存 |
| LOW | リソース過剰 | encoder `requests 2cpu/4Gi` / `limits 4cpu/8Gi` — 学習リポの最小スペック方針違反 (User memory) |

各項目の修正計画を §1 - §4 に記す。

---

## 1. serving 疎通 — KServe encoder / reranker の protocol mismatch (CRITICAL)

Phase 6 の KServe 切り替えで **4 箇所の契約不一致** が積み重なっている。搭載してもまず `/search` が 4xx/5xx になる。

### 1.1 Encoder payload schema の不一致

- **client** (`app/services/adapters/kserve_prediction.py:60`): `{"instances": [{"text": f"{kind}: {text.strip()}"}]}` — `kind` を前置詞として text に埋め込み、`kind` フィールドは送らない
- **server** (`ml/serving/encoder.py:73-79`): `EncoderInstance` が `text: str` + `kind: Field(pattern="^(query|passage)$")` を **両方必須** として Pydantic validation
- ⇒ 422 Unprocessable Entity。server は `kind == "query"` で分岐して `encode_queries` / `encode_passages` を切替えるロジックなので、text 先頭の `query:` 接頭辞は無視される (= 無害ではなく、contract 違反で落ちる)
- 付随: `tests/unit/app/test_adapters.py:70-74` が **client の壊れた shape をそのまま期待している**。テストをフィクスすると client の修正も強制される

**修正方針**:
1. `KServeEncoder.embed()` を `{"instances": [{"text": text.strip(), "kind": kind}]}` に戻す (Phase 5 `VertexEndpointEncoder` と同じ payload)
2. `tests/unit/app/test_adapters.py` の `sent_json` assert を対応する shape に修正
3. ME5 の `query: ` / `passage: ` 接頭辞は **server 側** (`E5Encoder.encode_queries` / `encode_passages`) が適用する契約を維持 (Phase 5 の `app/services/adapters/vertex_prediction.py` のコメントが正)

### 1.2 Encoder URL path の不一致

- **client** (`infra/manifests/search-api/deployment.yaml`): `KSERVE_ENCODER_URL = http://property-encoder.kserve-inference.svc.cluster.local/v1/models/property-encoder:predict`
- **server** (`ml/serving/encoder.py:121,126`): `@app.get(os.getenv("AIP_HEALTH_ROUTE", "/health"))` / `@app.post(os.getenv("AIP_PREDICT_ROUTE", "/predict"))`
- KServe custom-container (RawDeployment) は **pass-through** なので、Service DNS はそのまま container port 8080 に届くが、container は `/predict` しか listen しない
- ⇒ 404 Not Found

**修正方針**: どちらかに寄せる。学習容易性優先なら選択肢 A を推奨。

- **選択肢 A (推奨)**: deployment.yaml で `KSERVE_ENCODER_URL` を `.../predict` に変更。server は Vertex CPR 規約 (AIP_*) のまま
- **選択肢 B**: KServe 規約に寄せるため、encoder 側 Deployment (= `infra/manifests/kserve/encoder.yaml` の `env`) に `AIP_PREDICT_ROUTE=/v1/models/property-encoder:predict` と `AIP_HEALTH_ROUTE=/v1/models/property-encoder` を追加

reranker (MLServer LightGBM runtime) はフレームワーク側で `/v1/models/<name>:predict` を実装するので、reranker 側は URL 変更しない。つまり encoder 側のみ揃える。

### 1.3 Encoder STORAGE_URI の env var 名不一致

- **server** (`ml/serving/encoder.py:104`): `os.getenv("AIP_STORAGE_URI", "").strip()` を要求。empty の場合 `RuntimeError("AIP_STORAGE_URI is required")` で startup 失敗
- **manifest** (`infra/manifests/kserve/encoder.yaml:34`): `env: - name: STORAGE_URI`
- **patcher** (`scripts/local/deploy/kserve_models.py:107`): `{"name": "STORAGE_URI", "value": storage_uri}`
- ⇒ Pod CrashLoopBackOff

**修正方針**: encoder.yaml と `kserve_models.py::_patch_encoder_storage_uri` の env var 名を `STORAGE_URI` → `AIP_STORAGE_URI` に統一。server 側の env var 名は変更しない (Phase 5 encoder server を継承しているため)。

### 1.4 Encoder storageUri placeholder の trailing slash

- `infra/manifests/kserve/encoder.yaml:36`: `STORAGE_URI: "gs://mlops-dev-a-models/encoder/latest"` (trailing slash なし)
- `ml/serving/encoder.py:90-91`: `if prefix.prefix and not prefix.prefix.endswith("/"): raise RuntimeError("AIP_STORAGE_URI must point to a directory prefix")`
- ⇒ placeholder でも deploy_kserve_models 前でも Pod startup が failed。また `kserve_models.py` が Vertex Model Registry から取った `artifact_uri` も trailing slash 保証なし

**修正方針**:
1. encoder.yaml の placeholder を `gs://mlops-dev-a-models/encoder/latest/` (trailing slash つき) に変更
2. `scripts/local/deploy/kserve_models.py::_patch_encoder_storage_uri` で `storage_uri.rstrip("/") + "/"` と正規化してから patch

---

## 2. CI — `test_retrain.py` の fake clock 回帰 (CRITICAL)

- `tests/unit/app/test_retrain.py:34` が `if end > datetime.now(timezone.utc) - timedelta(days=5)` と wall-clock 依存になっている
- Phase 5 は `if end > NOW - timedelta(days=5)` (テスト固定値) で成立
- ⇒ `test_ndcg_drop_triggers_retrain` / `test_custom_ndcg_threshold_flips_decision` / `test_decision_exposes_ranker_fields` の 3 tests が FAIL
- `make check` が常に赤。CI の path filter は変わっていないため deploy 系 workflow にも波及する

**修正方針**: Phase 5 の実装 (lines 31-34 with `NOW` 固定値) を一字一句戻す。`evaluate()` の production code は両 phase で identical なので、テストだけ直せば通る。

---

## 3. Docker build context の巻き込み事故再発 (CRITICAL)

`.dockerignore` が Phase 5 の **minimal-whitelist 方針** から Phase 6 で **broad-exclude 方針** に退行している。User memory (`feedback_dockerignore_policy.md`) の事故多発ポイントに該当。

| Phase 5 (working) | Phase 6 (current, 危険) |
|---|---|
| Python cache / TF state / `.git` 系のみ除外 | `docs/`, `infra/`, `**/data/`, `**/tests/`, `**/*.md` 等を包括除外 |
| 過去事故のコメント (16行) で意図明示 | コメント削除 |

`**/data/` は `ml/data/` (seed CSV) を巻き込み、`infra/` は build context の依存ファイルと衝突するパターン。Phase 5 ではこれで 2 度の container startup 連鎖失敗があった (コメントに記録あり)。

**修正方針**: Phase 5 の `.dockerignore` をそのまま復元 (改変せずコピー)。コメントの `Phase 5 debug:` を `Phase 5/6 共通:` に書き換えるだけに留める。

---

## 4. pipeline / image naming 回帰 (HIGH)

### 4.1 `train_reranker` の Output artifact 復活

- `pipeline/training_job/components/train_reranker.py`: `def train(...) -> None:` + `model: dsl.Output[dsl.Model]` に戻った
- Phase 5 は `def train(...) -> str:` にして `model_artifact_uri` を string で返す workaround を入れていた。コメント:
  > Phase 5 encountered a KFP executor issue where Input artifacts caused worker to exit before emitting logs
- `main.py:93` も `model_artifact_uri=train_task.outputs["Output"]` → `model_artifact=train_task.outputs["model"]` に戻った
- ⇒ Phase 5 が直したばかりの KFP executor バグに再突入する可能性

**修正方針**: Phase 5 の workaround を維持。`train_reranker.py` / `register_reranker.py` / `main.py` の 3 ファイルを Phase 5 の実装に戻す。**ただし** `register_reranker.py` の MINIMAL-REGISTER stub は Phase 5 のデバッグ残骸なので **それだけは** Phase 6 版 (`aiplatform.Model.upload` を本実装) を採用する。

要するに:
- `train_reranker.py` → Phase 5 形式 (`-> str`)
- `main.py` の pipeline definition → Phase 5 形式 (`model_artifact_uri=train_task.outputs["Output"]`)
- `register_reranker.py` の引数 → Phase 5 形式 (`model_artifact_uri: str`) かつ実装は Phase 6 の本実装
- `evaluate.py` / `vizier.py` / `load_features.py` の STEP 1/2 debug logging は Phase 5 に揃えて復活 (KFP component 障害時のトリアージに load-bearing)

### 4.2 Container image 名 (ハイフン落ち)

- Phase 5: `asia-northeast1-docker.pkg.dev/mlops-dev-a/mlops/property-trainer:latest` / `property-reranker:latest`
- Phase 6: `trainer:latest` / `reranker:latest`
- `.github/workflows/deploy-trainer-image.yml` / `deploy-reranker-image.yml` / `deploy-encoder-image.yml` が push する tag と不一致なら、pipeline submit 時に Artifact Registry から pull できない

**修正方針**: `pipeline/training_job/main.py:38,48` と `pipeline/workflow/compile.py:44` の image 名を `property-trainer` / `property-reranker` / `property-encoder` に戻す。実 Artifact Registry の repository 名を確認して調整する (`make ops-docker-tags` 相当のコマンドか、`gcloud artifacts docker images list` で確認)。

---

## 5. scripts/config 回帰 (MED)

### 5.1 `_common.py::_load_yaml` の list-item 処理

- Phase 5: `if key.startswith("-"): continue` + `if value:` (value 必須) ガードあり
- Phase 6: `if key:` のみ。`- user1@example.com` のような YAML list item を key として拾ってしまう
- Phase 6 の setting.yaml は `admin_user_emails:` block を削除しているので現状は被害ゼロだが、再追加した瞬間に壊れる

### 5.2 `_load_list_setting` 関数の削除

- Phase 5 の helper。block-style list を読むために `_common.py::_load_list_setting("admin_user_emails")` を提供
- Phase 6 で関数ごと削除
- 今の Phase 6 setting.yaml では使用箇所なし (grep で 0 hit) なので即座には壊れないが、復旧容易性のため削除理由を明示するか、関数を残しつつコメントで scope-out するか選ぶ

**修正方針**: Phase 5 の `_load_yaml` ガードを復元。`_load_list_setting` は Phase 6 で未使用のため削除は妥当だが、削除コミットに「admin_user_emails 運用廃止」の理由を記載。並行して `env/config/setting.yaml` の `admin_user_emails:` block 削除の意思決定を `docs/01_仕様と設計.md` の SA 設計節に反映する (Phase 5 では `sa-api` impersonation の開発者リストとして 2 時間ハマった注記つきで使われていた)。

---

## 6. deploy_all の死んだステップ (MED)

- `scripts/local/setup/deploy_all.py:29,205-207` が step 9 で `setup_model_monitoring_main()` を呼ぶ
- `scripts/local/setup/setup_model_monitoring.py` は JSON を stdout に print するだけで、リソースを作らない
- Phase 6 の非負制約 (`CLAUDE.md` / `§5.5`): **Vertex Model Monitoring v2 は Vertex Endpoint 前提のため KServe 化で失う**。`monitoring/validate_feature_skew.sql` (BQ 側 skew 検知) は残す
- ⇒ step 9 は無害だが mis-leading。削除して total=9 に繰り下げる

**修正方針**: `deploy_all.py` から step 9 を削除、`total = 9` に変更。`scripts/local/setup/setup_model_monitoring.py` は削除せず「BQ skew 監視設定の placeholder」として残し、doctring を「Phase 6 縮退: Vertex Monitoring v2 を扱わない」に書き換える。`.github/workflows/` 側で呼ばれていないか grep で確認。

---

## 7. 学習リポ最小スペック方針違反 (LOW)

User memory (`feedback_low_spec_learning.md`): **Vertex Endpoint / Cloud Run 等のリソース既定は min=1/max=1/小型マシン**。

| リソース | 現状 (Phase 6) | 推奨 | 根拠 |
|---|---|---|---|
| `infra/manifests/search-api/deployment.yaml` | `requests: 500m/1Gi`, `limits: 2/2Gi`, replicas=1 | そのまま (CLAUDE.md 非負制約表に「Phase 5 Cloud Run の写像」として固定) | 非負制約 |
| `infra/manifests/search-api/hpa.yaml` | `minReplicas 1 / maxReplicas 10` | そのまま (非負制約) | 非負制約 |
| `infra/manifests/kserve/encoder.yaml` | `requests: 2cpu/4Gi, limits: 4cpu/8Gi`, `minReplicas: 1 maxReplicas: 3` | `requests: 1cpu/2Gi, limits: 2cpu/4Gi`, `minReplicas: 1 maxReplicas: 1` | ME5 (multilingual-e5-large) は 560M params / 560MB メモリで推論可。学習リポは評価実行だけなので max=1 で十分 |
| `infra/manifests/kserve/reranker.yaml` | `requests: 500m/1Gi, limits: 2/4Gi`, `maxReplicas: 5` | `requests: 250m/512Mi, limits: 1cpu/1Gi`, `maxReplicas: 1` | LightGBM model.txt は <10MB |

**修正方針**: CLAUDE.md 非負制約表との整合を取りつつ、KServe 側 (encoder/reranker) の `max{Replicas,Resources}` を学習用最小に絞る。変更は User flag。現時点では `TODO: User 合意待ち` として本ドキュメントに残し、コードは触らない。

---

## 8. 未対応項目リスト (実 GCP 検証系)

§1 - §7 を修正した後の、実 GCP 疎通で初めて確認できる項目。Phase 5 の旧ロードマップを Phase 6 の状況にフォーク。

### 8.1 実 GCP Infra apply
- [ ] 2 段階 apply: `terraform apply -target=module.iam -target=module.data -target=module.gke` → provider.tf 再有効化 → `terraform apply` で KServe / manifests を展開。`dev/main.tf` コメントの順序どおり
- [ ] `module.kserve` の `helm_release.kserve_crd` → `helm_release.kserve` が `deploymentMode=RawDeployment` で入ることを確認
- [ ] 10 SA が Phase 5 から継承されていること (`google_service_account` × 10) + GKE KSA × 3 の Workload Identity binding

### 8.2 serving 疎通 (§1 修正後の本番確認)
- [ ] `kubectl apply -k infra/manifests/` で Deployment / InferenceService / NetworkPolicy / Gateway / HPA / BackendConfig が 1-shot で揃う
- [ ] `make deploy-kserve-models` が Model Registry の `production` alias artifact URI を引いて encoder / reranker の storageUri を patch
- [ ] `make deploy-api` (Cloud Build + `kubectl set image`) で search-api が placeholder から Artifact Registry image に置換
- [ ] search-api Pod → KServe encoder/reranker の cluster-local HTTP 疎通 (`/search` E2E)
- [ ] Gateway 外部 URL + IAP allowlist 経由で `sa-scheduler` から `/jobs/check-retrain` が成功

### 8.3 Pipeline 本番疎通 (Phase 5 継承、Phase 6 で変更なし)
- [ ] `embed_pipeline` submit → 完走
- [ ] `train_pipeline` submit → 完走 (§4.1 修正後に再検証)
- [ ] `train_pipeline` の NDCG ゲート実挙動
- [ ] Model Registry の `staging` / `production` alias 運用

### 8.4 監視・再学習トリガー
- [ ] `monitoring/validate_feature_skew.sql` (BQ 側 skew 検知) の実アラート発火
- [ ] `model-monitoring-alerts` → Eventarc → Cloud Function → Pipeline submit は **Phase 6 ではスコープアウト** (§6)。将来 Prometheus `PodMonitoring` + Managed Prometheus alert に差し替える RFC を `docs/01_仕様と設計.md` に起票する
- [ ] Scheduler 起点 (Gateway + IAP 経由) の `/jobs/check-retrain` 自動実行

### 8.5 運用・品質確認
- [ ] `make deploy-all` / `make destroy-all` の Phase 6 実 GCP PDCA
- [ ] `docs/04_運用.md` の STEP 1-17 を実際に上から叩いて所要時間 / 失敗ケースを記録
- [ ] コスト観測: GKE Autopilot 定常 + KServe encoder (ME5 ロード) + BQ storage + Meilisearch Cloud Run

---

## 9. 作業順序 (推奨)

1. **§2 (test_retrain.py の fake clock 復元)** — 最も小さく `make check` を green に戻せる
2. **§3 (.dockerignore 復元)** — 1 ファイル置換。Docker build の土台を戻す
3. **§5 (scripts/_common.py ガード復元)** — 回帰防止。影響範囲小
4. **§1 (KServe protocol mismatch 4 件)** — 本 phase の最重要。修正後は `tests/unit/app/test_adapters.py` も同時に直すこと (feature parity の思想で同一 PR)
5. **§4 (pipeline / image 名回帰)** — Phase 5 の workaround を取り戻す
6. **§6 (deploy_all の dead step 除去)** — cleanup
7. **§7 (最小スペック化)** — User 合意待ち。本ドキュメントに `TODO` として残す
8. **§8 (実 GCP 疎通)** — 静的修正が全て入った後の最終検証

§1-§6 は全て静的検証で落ちる/本番で即 fail する defect なので、まとめて 1 PR で片付けて `make check` が green になるまで持ち込むのが効率的。

---

## 10. 整理ルール (Phase 5 から継承)

- [ ] 対応した項目は本ファイルから削除し、未対応のみ維持する
- [ ] 新しい未対応が発生した場合のみ本ファイルへ追記する
- [ ] `01_仕様と設計.md` / `03_実装カタログ.md` / `04_運用.md` / `CLAUDE.md` と矛盾した場合は本ファイルを正とし、他を追随させる (`docs/README.md §2`)
