# Cloud Build config for the batch embedding job image.

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=infra/run/jobs/embedding/Dockerfile
      - --tag=${_URI}
      - .
images:
  - ${_URI}

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _URI: ""
