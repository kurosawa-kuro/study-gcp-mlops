# 02_移行ロードマップ切り替え基盤 (残作業のみ)

> **状態**: ✅ **対応済み**。`scripts._common.resolve_api_target()` を canonical 実装として導入し、`scripts/ops/{livez,search,search_components,ranking,feedback,check_retrain,label_seed,accuracy_report}` 全 8 本が `TARGET=local|gcp` + 任意 `API_URL` 上書きで切替動作することを確認 (Phase 7 Run 1)。`tests/unit/scripts/test_resolve_api_target.py` で解決順序 (explicit → local → gcp) と token 発行条件を pin 済。
>
> 現行仕様の説明は本ドキュメントには残さない。canonical な仕様は実装と test に集約:
> - `scripts/_common.py::ResolvedApiTarget` / `resolve_api_target()`
> - `tests/unit/scripts/test_resolve_api_target.py` (規約の pin)
> - 利用例は `docs/04_運用.md` の各 `make ops-*` STEP

---

## 残作業 (P2、運用熟成後に判断)

| # | 項目 | 判断材料 |
|---|---|---|
| 1 | Phase 7 用 Gateway URL resolver の要否 | 現状 `make ops-*` は port-forward + `TARGET=local` で完走できるので即時必要性は低い。Gateway TLS Secret が整備されて外部公開が標準化したら検討 |
| 2 | `ops-*` の canonical 実行例を `docs/04_運用.md` に反映 | port-forward / `TARGET=local LOCAL_API_URL=http://127.0.0.1:18080` のパターンを 1 箇所に集約 |
| 3 | `API_URL` 明示運用と Gateway 自動解決のどちらを正本にするか | Phase 7 完了条件として: (a) `cloud_run_url()` 相当の GKE Gateway resolver を `gcp` mode に組む / (b) ops 実行時は `API_URL` 明示を canonical にする — のどちらかに統一 |

いずれも「動かなくなった」事象ではなく「磨き込み」案件のため、Phase 7 主検証 (`/search` 3 成分 + `/rag` + `/search?explain=true`) には影響しない。
