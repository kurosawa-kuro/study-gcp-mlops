# 04_検証.md — Phase 7 (GKE + KServe) 検証ガイド

> 注記: 「**何を見れば 'OK' とみなしてよいか**」を集約したドキュメント。`docs/05_運用.md` (PDCA / 個別 STEP / 定常運用) とは性質が異なる:
>
> - `05_運用.md` = **やる** (`make deploy-all` を回す手順、定常監視、インシデント対応)
> - `04_検証.md` = **OK か判定する** (ローカル / GCP 検証ゲート、退化検知、recovery)
>
> 順序関係: **本書 → `05_運用.md`**。変更後は本書の §2 (ローカル) → §3 (GCP ゲート、特に G3-G5) を全部 PASS させる。検証ゲートを通った run のみが `05_運用.md §3` の定常運用に進む資格を持つ。途中でエラーが出たら §4 (一過性エラー) か `05_運用.md §1.6` (恒久対処済ハマり) に該当するか判定する。
>
> PDCA を回すフロー自体 (`make deploy-all` / `make destroy-all` 等の操作手順) は `05_運用.md §1` を参照。
>
> **最新仕様での判定原則**: Phase 7 の「OK」は、単に API が 200 を返すことではない。**Vertex Vector Search + Feature Online Store + Composer 本線の canonical 1 経路で動作し、BigQuery fallback / backend 切替 flag / legacy env alias / legacy route / 旧 Vertex Endpoint shell への依存が検証対象から外れていること**まで含めて OK とする。

---

## 0. 現在の検証状況と重点未済項目 (2026-05-03 夕 更新)

このセクションは **「今 sprint で何が検証済みで、何が未検証なのか」** の最新スナップショット。PDCA 1 周を回しきるまでは本節を最初に読む。完了したら §5.1 (最新実測ログ) に統合し、本節は次回検証 sprint まで空にする。

### 0.1 検証済み (offline、コード変更で fix できる範囲) ✅

| 区分 | 内容 | 証跡 |
|---|---|---|
| L1 静的検証 | `make check` 649 PASS / 1 skipped | `make check` 出力 |
| L1' レイヤ境界 | `make check-layers` PASS | `make check-layers` 出力 |
| L1'' legacy residue | W2-8 完了 (BQ adapter / backend 切替 env 削除済) | `rg` で 0 hit |
| TF 静的検証 | `make tf-validate` Success | `make tf-validate` 出力 |
| destroy-all 契約 | contract test 15 PASS (旧 9 + incident 3 + state_recovery 3) | [`tests/integration/workflow/test_destroy_all_contract.py`](../../tests/integration/workflow/test_destroy_all_contract.py) |
| state_recovery 12 type 実装 | `_recover_iam_sas` / `_recover_bq` / `_recover_pubsub` / `_recover_cloudfunctions` / `_recover_eventarc` / `_recover_cloud_run` / `_recover_artifact_registry` / `_recover_secret_manager` / `_recover_dataform` / `_recover_gcs_buckets` / `_recover_feature_store` (FG/FOS/FV) / `FEATURE_GROUP_FEATURES` (7 個) | [`scripts/infra/state_recovery.py`](../../scripts/infra/state_recovery.py) |
| runbook §1.4-emergency | 緊急 kill switch + tfstate orphan cleanup + `make state-recover` 推奨 | [`docs/runbook/05_運用.md`](05_運用.md) |
| tfstate orphan cleanup | 151 entries → 0 達成 | 2026-05-03 昼実施 |

### 0.2 **重点未済 (live verify、本 sprint の最終ゲート)** 🔄

優先順位の高い順 (上から潰す):

| # | 項目 | コマンド | 完了基準 | 現状 |
|---|---|---|---|---|
| **V1** | `make deploy-all` **完走** (state_recovery 12 type 完備版) | `make deploy-all` | step 1-15 全 PASS、`Apply complete` で終了、Composer/GKE/VVS/Feature Store がすべて立ち上がる | **Run 6 進行中** (Run 1-5 で `alreadyExists` を incremental に発見・吸収。Run 6 は 12 type 完備で初の完走チャレンジ) |
| **V2** | `make destroy-all` 完走 (state rm + import pattern の live 検証) | `make destroy-all` | (a) `[2/6++] state rm 永続化 VVS` ログが出る (b) `[6/6]` 本体 destroy が complete (c) GCP 上に Index/Endpoint だけ残る | **未着手** (V1 完走待ち) |
| **V3** | 2 周目 deploy-all (`terraform import` 経路の live 検証) | `make deploy-all` (V2 直後) | step 6 で `terraform import` ログが出て deployed_index のみ create される、所要 30 分 → **10-15 分** (§4.9 期待効果) | **未着手** (V2 完走待ち) |
| **V4** | G3 3 種コンポーネント | `make ops-search-components` | `lexical=N1 semantic=N2 rerank=N3` 全 non-zero | **未着手** (V1 完走待ち) |
| **V5** | G4 canonical semantic / feature path | `make ops-vertex-vector-search-smoke` + `make ops-vertex-feature-group` | VVS `find_neighbors` 返却、Feature View 経由 fetch 返却 | **未着手** (V1 完走待ち) |
| **V6** | G5 互換レイヤ撤去確認 | `kubectl get configmap -n search search-api-config -o yaml` | `SEMANTIC_BACKEND` / `FEATURE_FETCHER_BACKEND` が manifest・config から消えている | **未着手** (V1 完走待ち)、コード上は W2-8 で削除済 |
| **V7** | G6 retrain pipeline | `make ops-train-now` + `make ops-train-wait` | `PIPELINE_STATE_SUCCEEDED` (~5-10 分) | **未着手** (V1 完走待ち) |
| **V8** | G8 精度 | `make ops-accuracy-report` | `ndcg_at_10 >= 1.0` (5 件 seed では満点) | **未着手** (V1 完走待ち) |
| **V9** | Composer DAG SUCCEEDED | `make ops-composer-trigger DAG=retrain_orchestration` | Airflow UI で SUCCEEDED | **後追い候補** (DAG bag 登録済、live SUCCEEDED 未確認、深追いは別 sprint 候補 — §4.1) |
| **V10** | parity live | `tests/integration/parity/*` `live_gcp` mark | live PASS | **別 session 妥当** (重要度低、W2-8 後の cross-check) |

### 0.3 V1 fail 時の分岐 (state_recovery で吸収できなかった新 type)

V1 (deploy-all 完走) で `alreadyExists` 系 error が新たに出た場合の対処手順:

1. error 出力から GCP resource type を identify (`google_<type>` を grep)
2. [`scripts/infra/state_recovery.py`](../../scripts/infra/state_recovery.py) に `_recover_<type>` helper を追加
3. `recover_orphan_gcp_resources` の entrypoint に呼出しを追加
4. [`tests/integration/workflow/test_destroy_all_contract.py::test_deploy_all_invokes_state_recovery_before_tf_apply`](../../tests/integration/workflow/test_destroy_all_contract.py) の helper list を更新
5. `make check` PASS → `make deploy-all` で再 attempt

これは Run 1-5 で実施した手順と同じ。Run 6 が fail したら本 procedure で **Run 7+** を回す。

### 0.4 なぜローカルで完結できないのか (再掲、§2 L5 の補足)

**ローカルで弾けるはずの失敗はローカルで弾く** が原則だが、以下は **GCP 実機でしか観測不可**:

- `alreadyExists` 系 error (= GCP 残置 resource との衝突): 実 GCP project state に依存
- VVS deployed index attach (~26 min): GCP managed operation
- Composer 環境作成 (~20 min): GCP managed operation
- KServe `production` alias 切替: cluster-side
- 3 種 (lexical/semantic/rerank) all non-zero: cluster-side serving

= V1-V10 はすべて live verify 必須。本 sprint 内で V1-V8 完走、V9-V10 は別 sprint 妥当。

---

## 1. 検証の考え方

PDCA を回す前 / 回しきった後に「何を見れば 'OK' とみなしてよいか」の checklist。**ローカルで早く落とせる失敗はローカルで落とし、cluster 依存は GCP 側で確認する**。加えて、**教育コードとしての完成条件は canonical 1 経路への収束**なので、「互換レイヤが残っていないこと」自体も検証対象に含める。

階層別の責務:

| 層 | 何が確認できる | 所要時間 | 認証 |
|---|---|---|---|
| ローカル (§2) | import / boot / 静的検証 / image 構成 / obvious legacy residue | 1-5 分 | ADC のみ (任意) |
| GCP 検証ゲート (§3) | canonical 経路の 3 種が all non-zero、NDCG@10、Gateway TLS、KServe Workload Identity、互換レイヤ不要の確認 | 3-10 分 (deploy 後) | フル GCP |
| 退化検知 (§5) | 性能ベースライン (build 時間、image size 等) との乖離 | 数分 | ローカル + GCP |

ローカルで弾けるはずの失敗が GCP に持ち込まれると、deploy-all を 1 回ぶん (~5 分) と Cloud Build コストが無駄になる → **§2 を先に通す**。

---

## 2. ローカル検証 (GCP 認証ゼロでも通せる範囲)

| # | 種類 | コマンド | OK 基準 | 落ちたら見る場所 |
|---|---|---|---|---|
| L1 | 静的検証 | `make check` (ruff / fmt / mypy / pytest) | 全 PASS | 直前の編集ファイル。新規 module は `scripts/ci/layers.py::DIRECTORY_RULES` に追記したか確認 |
| L1' | レイヤ境界 | `make check-layers` | `check-layers: OK (51 files clean)` | Port が adapter / SDK を import していないか (`scripts/ci/layers.py`) |
| L1'' | legacy residue grep | `rg -n "BigQuerySemanticSearch|BigQueryFeatureFetcher|SEMANTIC_BACKEND|FEATURE_FETCHER_BACKEND|VERTEX_FEATURE_GROUP_ID|ui_model_metrics_legacy|ui_data_legacy|enable_vertex_endpoint_shell|vertex_encoder_endpoint_id|vertex_reranker_endpoint_id" app infra scripts docs` | **W2-8 完了後は 0 hit**。W2-7 までは既知の暫定残骸として差分管理 | 互換レイヤ削除漏れ。`docs/02_移行ロードマップ.md §4.6` の削除対象一覧と突合 |
| L2 | image build | `DOCKER_BUILDKIT=1 docker build -f infra/run/services/search_api/Dockerfile -t local-test/search-api:dev .` | image size **~700MB** (3GB を超えていたら `[ml]` extra が混入している) | `pyproject.toml` の `[project.optional-dependencies]` から `lightgbm` / `sentence-transformers` / `kfp` が漏れて base に戻っていないか |
| L3 | image 起動 (import 検証) | `docker run --rm --entrypoint=python local-test/search-api:dev -c "from app.main import app; import scripts._common; import scripts.ops.destroy_check"` | エラーなく完了 | `Dockerfile` で `scripts/` の COPY が抜けていると `ModuleNotFoundError: scripts` で落ちる (Phase 7 Run 5 の事故ケース、現在は対処済) |
| L4 | container 起動 (`/livez`) | `docker run --rm -d --name phase7-local-contract -e ENABLE_SEARCH=false -e PROJECT_ID=mlops-dev-a -e REGION=asia-northeast1 local-test/search-api:dev` → `docker exec phase7-local-contract python -c "import urllib.request; r=urllib.request.urlopen('http://127.0.0.1:8080/livez', timeout=5); print(r.status); print(r.read().decode())"` | `200` + `{"status":"ok"}` | **`ENABLE_SEARCH=false` では ADC 不要が contract**。ここで ADC や BigQuery/Pub/Sub 初期化を要求したら local boot regression |
| L5 | ハイブリッド検索 3 種 (lexical / semantic / rerank) のローカル統合 | (実施しない) | — | **3 系統の canonical 統合はローカル単体では不可**。Meilisearch の lexical 局所確認はローカルでも可能だが、Vertex Vector Search / Feature Online Store / KServe を含む本線成立は GCP / cluster 依存。`make ops-search-components` と §3 G3-G5 で実機検証する |

L5 が「ローカルだけで完結しない」のは設計上の制約。**lexical 単体の Meilisearch はローカルで局所確認できる** 一方、Phase 7 の完成条件は Meilisearch だけではなく、Vertex Vector Search / Feature View / KServe reranker を含む canonical 1 経路での成立にある。そもそも Meilisearch を採用している理由の 1 つが、**lexical 検索だけはローカルでも触って学べるようにすること**にある。Phase 6 (Cloud Run 単体) は in-process reranker でローカル統合できたが、Phase 7 は serving 層を KServe に移譲した結果、reranker が cluster-side HTTP 呼び出しに変わった。意図的なトレードオフ:

- **ローカルで早く回せるもの**: import / boot / 静的検証、Meilisearch の lexical 局所確認
- **実機でしか回せないもの**: 3 種が all non-zero、Vertex Vector Search 実検索、Feature View 経由 fetch、互換レイヤ不要の確認、NDCG@10、Gateway TLS、KServe Workload Identity

---

## 3. GCP 検証サイクル (PDCA = `05_運用.md §1.2` + 検証ゲート)

`make deploy-all` 完了後 → `make run-all-core` の各 step が以下のゲートになる。**最新仕様では「3 種が non-zero」だけでは不十分で、canonical 経路に乗っていることと、互換レイヤが不要になっていることも確認する**:

| # | step | コマンド | OK 基準 | 失敗時のメンタル分岐 |
|---|---|---|---|---|
| G1 | レイヤ境界 (再確認) | `make check-layers` | OK | (ローカルで弾けるはず — §2 L1' 参照) |
| G2 | seed | `make seed-test` | `5 properties materialised` | BQ table がまだ deletion_protection で守られているか、SA に `bigquery.dataEditor` が無い |
| G2' | meili sync | `make sync-meili` | `meili.tasks` API が 200 | Meilisearch Cloud Run の `meili-master-key` 未投入。`05_運用.md §1.6` 参照 |
| G3 | **3 種コンポーネント (load-bearing)** | `make ops-search-components` | `[component-check] hits lexical=N1 semantic=N2 rerank=N3` で **全て non-zero** | `lexical=0` → Meilisearch index 未同期 (G2' 確認)。`semantic=0` → VVS index 未投入 / endpoint 未配線。`rerank=0` → KServe `property-reranker` が `production` alias 未設定、または `make deploy-kserve-models` 未実行 |
| G4 | **canonical semantic / feature path** | `make ops-vertex-vector-search-smoke` + `make ops-vertex-feature-group` | VVS `find_neighbors` が返る、Feature View 経由 fetch が返る | VVS endpoint / deployed index / backfill 未完了、または Feature Online Store endpoint / view / sync 未完了 |
| G5 | **互換レイヤ撤去確認** | `kubectl get configmap -n search search-api-config -o yaml` / `rg` / `terraform output` | **W2-8 完了後**: `SEMANTIC_BACKEND` / `FEATURE_FETCHER_BACKEND` / legacy alias が manifest・config・docs から消えている | 互換レイヤ削除漏れ。`docs/02_移行ロードマップ.md §4.6` の削除対象と照合 |
| G6 | retrain pipeline | `make ops-train-now` → `make ops-train-wait` | latest `property-search-train` が `PIPELINE_STATE_SUCCEEDED` (~5-10 分) | Vertex AI Pipeline 履歴 (`make ops-vertex-pipeline-status`) で個別 step エラーを確認 |
| G7 | feedback / ranking smoke | `make ops-feedback` / `ops-ranking` | `feedback_accepted: true`、ranking JSON に `lexical_rank` / `semantic_rank` / `score` が並ぶ | search-api pod log (`kubectl logs -n search deployment/search-api --tail=50`) |
| G8 | 精度 | `make ops-accuracy-report` | `ndcg_at_10 >= 1.0` (5 件 seed では満点) | seed が想定通り入ったか (G2)、ranking 関数の degradation はないか |

**G3 + G4 + G5 が最重要ゲート**: ハイブリッド検索の 3 構成要素 (BM25 / Vertex Vector Search / LightGBM 再ランク) が全て寄与し、さらに Feature View 経由 fetch と互換レイヤ撤去が確認できて初めてその run は使える。どれかが落ちたら**他のメトリクスがどれだけ良くてもその run は使えない**。

---

## 4. 検証中に出やすい一過性エラーと recovery

PDCA で実測したエラー (恒久対処済ではなく、**遭遇したらリトライ手順を踏む**もの)。コード修正済の永続的ハマりは `05_運用.md §1.6` を参照。

| 症状 | 発生フェーズ | 一次原因 | recovery |
|---|---|---|---|
| `Error waiting to create Service: ... container failed to start and listen on PORT=8080` (Meilisearch Cloud Run) | `terraform apply` 中 | 既存 service が部分作成状態 (cold-start race / image pull 失敗) | `gcloud run services delete meili-search --region=asia-northeast1 --quiet` で消してから `make deploy-all` 再実行 (Terraform が冪等で再作成) |
| `Error trying to delete bucket mlops-dev-a-pipeline-root containing objects without 'force_destroy' set to true` | `make destroy-all` 終盤 | run-all 中の Vertex Pipeline が `gs://...-pipeline-root/runs/<job-id>/` に artifact を書き込んだ後、destroy-all stage [3/6] の bucket wipe 後に新規 artifact が来た | `gcloud storage rm -r gs://mlops-dev-a-pipeline-root/runs/` → `make destroy-all` 再実行 |
| `cannot destroy table ... property_features_online_latest without setting deletion_protection=false` | `make destroy-all` 本体 destroy | online serving view を追加したのに `scripts/setup/destroy_all.py::PROTECTED_TARGETS` へ未反映 | `PROTECTED_TARGETS` に `module.data.google_bigquery_table.property_features_online_latest` を追加し、`tests/integration/infra/test_destroy_all_table_parity.py` baseline を更新 |
| `URLError: Connection reset by peer` on `make ops-livez` | `make run-all-core` 開始直後 | Gateway PROGRAMMED 完了前 / managed cert 発行中 | 60 秒待機 → `kubectl get gateway -n search` で `PROGRAMMED: True` を確認してから再実行 |
| `TimeoutError: The read operation timed out` on `make ops-search` | `make run-all-core` 中盤 (rolling restart 直後 / KServe cold start 中) | search-api Pod もしくは KServe predictor の cold-start で `/search` 応答が transient に遅延 | `scripts/ops/search.py` は `SEARCH_RETRIES` (default 3) / `SEARCH_RETRY_SLEEP` (default 2.0s) で透過リトライするため通常は再 invocation 不要。それでも継続 fail なら `kubectl get pods -n search,kserve-inference` と `kubectl logs deployment/search-api -n search` を確認 |
| Pod が `CrashLoopBackOff: ModuleNotFoundError: No module named 'scripts'` | rollout 直後 | search-api Dockerfile が `COPY scripts/` を欠いている (Phase 7 Run 5 で実害発生) | `infra/run/services/search_api/Dockerfile` の `COPY scripts/ scripts/` と runtime stage の `COPY --from=builder /src/scripts /home/app/scripts` が両方あるか確認 (現在の HEAD は対処済) |

**運用方針**: いずれも「同じコマンドをもう一度叩けば通る」もの。手動修復ループに入ったら `destroy-all` でクリーンスレートに戻すのが PDCA の trade-off。

---

## 5. 性能ベースライン (実測値、退化検知用)

最適化済 PDCA 1 サイクルの実測値 (2026-04-29、`mlops-dev-a` / `asia-northeast1`、WSL2):

| 指標 | ベースライン | 退化検知の目安 |
|---|---|---|
| `make destroy-all` (state 空) | <1s | — (空に対する no-op) |
| `make destroy-all` (フル稼働状態から) | ~10 分 | 15 分超 → namespace finalizer 詰まりを `kubectl get servicenetworkendpointgroups -A` で確認 |
| `make deploy-all` 全体 | **~30-40 分** (2026-05-02 実測では VVS deployed index attach 26m21s が支配) | 45 分超 → `module.vector_search.google_vertex_ai_index_endpoint_deployed_index.property_embeddings[0]` の long-running operation を確認 |
| Cloud Build (search-api、kaniko cache hit) | **~3 分** | 6 分超 → image に重い依存 (torch / kfp) が混入していないか `docker history` で確認 |
| kubectl rollout (search-api) | **~1 分** | 3 分超 → image pull が遅い (image size が大きい)。`docker manifest inspect <image>` で `sizeSum` を確認 |
| search-api image | **~705MB** (compressed) | 1.5GB 超 → `pyproject.toml` の base `dependencies` に `[ml]` / `[pipelines]` が混入していないか |
| `make run-all-core` 全体 | ~3 分 | — (実機 API 呼び出しがほとんどなので GCP latency 依存) |
| pytest (`make test`) | 449 passed in ~5s | 失敗時は新規依存 import を `pyproject.toml` に追加するか、test 側で skip マーカー |

### 5.1 最新実測ログ (2026-05-02, Phase 7 workflow contract 修正後)

`ENABLE_SEARCH=false` の local boot contract と、GCP 上の canonical serving path を実測で通した記録。workflow contract 修正後に最低限ここまで再実行してから次の PDCA に進む。

| 区分 | 実行コマンド | 実測結果 |
|---|---|---|
| Local build | `DOCKER_BUILDKIT=1 docker build -f infra/run/services/search_api/Dockerfile -t local-test/search-api:dev .` | 成功。image `sha256:915db7fa25501fd8f11c6666f86b892ec74c6c3c3bfa2fb7ea6d364d75e1b33e` |
| Local import smoke | `docker run --rm --entrypoint=python local-test/search-api:dev -c "from app.main import app; import scripts._common; import scripts.ops.destroy_check"` | 成功。`scripts/` import を含めて落ちない |
| Local `/livez` | `docker run --rm -d --name phase7-local-contract -e ENABLE_SEARCH=false -e PROJECT_ID=mlops-dev-a -e REGION=asia-northeast1 local-test/search-api:dev` + `docker exec phase7-local-contract python -c "import json, urllib.request; r=urllib.request.urlopen('http://127.0.0.1:8080/livez', timeout=5); print(r.status); print(r.read().decode())"` | `200` / `{"status":"ok"}`。ADC なしで boot 成立 |
| GCP `/livez` | `make ops-livez` | 成功。`{"status":"ok"}` |
| GCP one-shot deploy | `make deploy-all` | 成功。`tf-apply` stage1/stage2 を完走し、VVS deployed index attach は `26m21s` で完了 |
| GCP deploy tail rerun | `uv run python -m scripts.setup.deploy_all --from-step 8 --to-step 14` | 成功。`seed-test -> sync-meili -> backfill-vvs -> trigger-fv-sync -> apply-manifests -> overlay-configmap -> deploy-api` を live 再実行しても、その後 `ops-search-components` / `ops-vertex-vector-search-smoke` / `ops-vertex-feature-group` は全て PASS |
| GCP 3 種 gate | `make ops-search-components` | 成功。`lexical=1 semantic=3 rerank=5` |
| GCP VVS smoke | `make ops-vertex-vector-search-smoke` | 成功。endpoint `4579342784384729088`、deployed index `property_embeddings_v1`、5 neighbors |
| GCP Feature View sync | `uv run python -m scripts.infra.feature_view_sync` | 成功。sync `featureViewSyncs/8113658246843072512` 完了 |
| GCP FOS fetch | `make ops-vertex-feature-group` | 成功。public endpoint `1452050240013074432.asia-northeast1-941178142366.featurestore.vertexai.goog`、`property_id='p001'` に対して 7 features 取得 |
| GCP feedback smoke | `make ops-feedback` | 成功。`{"accepted": true}` |
| GCP ranking smoke | `make ops-ranking` | 成功。各 result に `lexical_rank` / `final_rank` / `score` / `me5_score` を確認 |
| GCP accuracy gate | `make ops-accuracy-report` | 成功。`ndcg_at_10=1.0` / `hit_rate_at_10=1.0` / `mrr_at_10=1.0` |
| GCP retrain wait | `make ops-train-now` + `make ops-train-wait` | 成功。`property-search-train-20260502164733` が `SUCCEEDED` |
| GCP `/search` smoke | `make ops-search` | 成功。reranker 経由で `final_rank` / `me5_score` 付き 5 件返却 (`SEARCH_RETRIES` 透過リトライ実装後の実測では retry 未発火で 1 発 PASS) |
| GCP `run-all-core` 完走 | `make run-all-core` | 成功 (exit 0)。`ops-livez → ops-search → ops-search-components → ops-vertex-vector-search-smoke → ops-vertex-feature-group → ops-feedback → ops-ranking → ops-train-now+wait → ops-label-seed → ops-daily → ops-accuracy-report` を全 PASS で通過。`ndcg_at_10=1.0 hit_rate_at_10=1.0 mrr_at_10=1.0` |

今回の live 修正で踏んだ根因:

- `deploy-all` が lexical lane (`sync-meili`) と semantic lane (`backfill-vvs`) を内包しておらず、完走後も `lexical=0` / `0 neighbors` になり得た
- `seed_minimal.py` が `feature_timestamp` を入れておらず、Feature View sync 前提を壊していた
- Feature View fetch 側が canonical resource name を使っていなかった
- Feature View source が online serving に不向きな schema を直接見ていた

恒久対処:

- `deploy-all` に `sync-meili` と `backfill-vvs` を追加し、`seed-test -> sync-meili -> backfill-vvs -> trigger-fv-sync` を workflow contract として固定
- seed に `feature_timestamp = CURRENT_TIMESTAMP()` を追加
- fetch 側で Admin API から canonical Feature View 名を解決
- online serving 用に `property_features_online_latest` view を追加し、Feature View source をそこへ固定

**ベースライン退化を疑うべき編集**:
- `pyproject.toml` の `dependencies` を増やした (特に `torch` / `tensorflow` / `kfp` 系)
- `Dockerfile` のレイヤ順を変えた (`COPY app/` を上に動かすと cache hit 率が崩れる)
- `cloudbuild.yaml` から kaniko を docker builder に戻した

---

## 6. 検証依頼プロンプト例 (Claude Code 向け)

「どの粒度で検証してほしいか」を明示すると、過不足のない作業範囲で動かせる。コピペで使える例:

### 6.1 ローカルだけ (1-5 分、GCP 認証不要)

```
ローカル検証だけ依頼。L1+L2 (make check + image build)
```
→ `make check-layers` → `make check` → `docker build` を回し、image size と test 結果を報告。

```
ローカル起動チェック依頼 (L4 まで)
```
→ 上に加えて `docker run` で `/livez` 200 確認まで。

### 6.2 単発 GCP 検証 (cluster は既に稼働中)

```
deploy 済みの cluster で 3 種 (lexical/semantic/rerank) チェック依頼。NDCG も
```
→ `make ops-search-components` + `make ops-vertex-vector-search-smoke` + `python -m scripts.ops.vertex.feature_group` + `make ops-accuracy-report` を叩いて結果報告。失敗時は §3 G3-G5 の分岐表に従って原因切り分け。

```
ops-search-components が rerank=0 になる原因切り分け依頼
```
→ KServe `production` alias / `make deploy-kserve-models` 状況 / pod log の順で診断。

### 6.3 フル PDCA (~30 分、cluster 全消し→再構築→検証→消す)

```
destroy-all, deploy-all, run-all, destroy-all の PDCA サイクル検証依頼
```
→ 4 step を順に実行、各 phase の elapsed と canonical ゲート (G3-G5) 結果を報告。

```
deploy-all + run-all-core だけ依頼 (destroy はしない、cluster 残す)
```
→ コストが残るため明示要求時のみ。run-all-core 完了後に「destroy しますか?」と確認。

### 6.4 変更後の影響確認 (退化検知)

```
Dockerfile の COPY 順を変えた。L1+L2+G3-G5 で退化検知依頼。
image size と build 時間が §5 のベースラインと乖離していないか確認
```
→ ローカル build → ベースライン照合 (705MB / kaniko cache hit ~3 分等) → 必要なら deploy して G3-G5 まで。

```
pyproject.toml の dependencies を増やした。重い依存が search-api image に
混入していないか確認依頼
```
→ ローカル build → `docker run --entrypoint=python ... -c "import torch, lightgbm, kfp"` が全て ImportError であることを検証 (§5 の退化検知項目)。

### 6.5 一過性エラー recovery

```
make deploy-all が Meilisearch Cloud Run health check で失敗。recovery 依頼
```
→ §4 の一次手順 (`gcloud run services delete meili-search` → `make deploy-all` 再実行)。

```
make destroy-all が pipeline-root bucket force_destroy エラー。クリーンアップ依頼
```
→ `gcloud storage rm -r gs://mlops-dev-a-pipeline-root/runs/` → `make destroy-all` 再実行。

### 6.6 計測 / ベースライン更新

```
今回の最適化で deploy-all 時間がどれだけ縮んだか計測依頼。
§5 のベースラインを実測値で更新
```
→ destroy → deploy → 各 phase elapsed 抽出 → 表を更新する PR を起こす。

### 6.7 短縮表記 (慣れた後に)

| 短縮 | 展開 |
|---|---|
| 「ローカル検証」 | §2 L1-L4 |
| 「3 種チェック」 | §3 G3 (`ops-search-components`) |
| 「canonical チェック」 | §3 G4-G5 (VVS / Feature View / 互換レイヤ撤去) |
| 「PDCA 検証」 | `05_運用.md §1.2` フルサイクル + §3 全ゲート |
| 「退化検知」 | §5 ベースライン照合 |
| 「§4 の Meili 復旧」 | §4 表の該当行手順 |

`§N` の番号で参照すると、表 (G3-G5 / 退化検知項目) を直接引けるので作業のレイテンシが下がる。
