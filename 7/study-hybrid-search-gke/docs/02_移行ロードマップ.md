# 02. 移行ロードマップ — 検索アプリを最新仕様へ

Phase 7 の現コードを、最新仕様 (親 [README.md](../../../README.md) §1-§3 / 親 [docs/01_仕様と設計.md](../../../docs/01_仕様と設計.md) / 本 phase [docs/01_仕様と設計.md](01_仕様と設計.md)) に追従させるための移行計画。

> **方針**: **Wave 1 = 検索アプリ自体 (app / ml / pipeline コード)** を先に整える。**Wave 2 = GCP インフラ (Terraform / IAM / deploy)** はその後。Wave 3 は docs / reference architecture との整合確認のみ (コード変更なし)。
>
> Port / Adapter / DI 大枠の整理は [`docs/02_移行ロードマップ-Port-Adapter-DI.md`](02_移行ロードマップ-Port-Adapter-DI.md)、過去の制約決定は [`docs/decisions/`](decisions/README.md) を参照。

## 0. 前提と非負ルール (作業前に必ず確認)

- **中核 5 要素は不変**: Meilisearch BM25 / multilingual-e5 / ベクトルストア (Phase 4 = BQ `VECTOR_SEARCH` / Phase 5+ = Vertex AI Vector Search) / RRF / LightGBM LambdaRank
- **default `/search` 挙動は壊さない**: 切替は環境変数 (Strangler パターン)、既定は現行挙動
- **embedding 生成履歴・メタデータの正本は BigQuery 側**: Vertex Vector Search は本番 serving index、source は BQ embedding テーブル (`feature_mart.property_embeddings`)
- **Feature Store (Phase 5 必須)** を Phase 7 でも継承: training-serving skew 防止のため、Feature Online Store 経路を Phase 7 固有 opt-in で持つ
- **Meilisearch / Redis cache は据え置き**: 実案件 reference architecture (Elasticsearch + Redis 同義語辞書) は本 phase でもコードに混入させない (本 phase docs/01 §4 参照)
- **Feature parity invariant 6 ファイル原則** は SemanticSearch / FeatureFetcher 変更でも継続 PASS

---

## 1. 現状コードと仕様のギャップ

| 観点 | 現状コード | 最新仕様 (target) | ギャップ |
|---|---|---|---|
| Semantic 検索 adapter | [`bigquery_semantic_search.py`](../app/services/adapters/bigquery_semantic_search.py) のみ (BQ `VECTOR_SEARCH`) | Vertex AI Vector Search を本番 serving index にする (Phase 5+ 仕様) | `VertexVectorSearchSemanticSearch` adapter 未実装 |
| `SemanticSearch` 切替 | composition_root に backend 切替なし、常に BQ | 環境変数 `SEMANTIC_BACKEND` で `bq` / `vertex_vector_search` を切替 | settings + composition root 未対応 |
| Feature 取得 (rerank 入力) | `app/services/ranking.py` から BigQuery 直読 (推定) | Phase 5 で Feature Online Store 経由 (training-serving skew 防止) | `FeatureFetcher` Port + `FeatureOnlineStoreFetcher` adapter 未実装 |
| KServe Reranker → Feature Online Store opt-in | 未実装 | Phase 7 固有として opt-in (default off) | 環境変数 + adapter 配線 未実装 |
| Embed pipeline の出力先 | `feature_mart.property_embeddings` (BQ) のみ | BQ + Vertex Vector Search index 双方に書く (BQ は正本、VVS は serving index) | `VectorSearchWriter` Port + adapter + pipeline component 未実装 |
| Vector Search Terraform モジュール | [`infra/terraform/modules/vector_search/`](../infra/terraform/modules/vector_search/) ディレクトリは存在するが空 | `google_vertex_ai_index` + `google_vertex_ai_index_endpoint` + deployed index | **Wave 2** (GCP infra) で実装 |
| Feature Online Store Terraform | 既に [`modules/vertex/main.tf:273`](../infra/terraform/modules/vertex/main.tf) に資源定義あり、`enable_feature_online_store` default = `false` | Phase 5 必須要素なので default `true` 化、ただし `mlops-dev-a` の PDCA 都合で env 切替可能 | **Wave 2** で flag 切替方針を整理 |
| docs reference architecture (Elasticsearch / Redis 同義語辞書) | コードに無い (✓ 期待通り) | 実装しない (docs only) | **Wave 3** で lint 化 (codeに固有名が漏れていないか確認) |

---

## 2. 移行戦略

### 2.1 Strangler パターン

既存 `BigQuerySemanticSearch` adapter を残したまま `VertexVectorSearchSemanticSearch` を新規追加。composition root が環境変数で切替。検証完了後に default を `vertex_vector_search` に切替、最終的に BQ adapter は Phase 4 専用として位置付ける (Phase 7 では deprecated 注釈、削除はしない)。

### 2.2 PR 分割粒度 (1 PR = 1 Port 原則)

| PR | スコープ | 受け入れ条件 |
|---|---|---|
| PR-1 | `SemanticSearch` Port + Vertex Vector Search adapter (app 層) + fake / unit test | `SEMANTIC_BACKEND=vertex_vector_search` で `/search` が in-memory fake 経由で 200 を返す |
| PR-2 | `FeatureFetcher` Port + Feature Online Store adapter (app 層) + fake | `FEATURE_FETCHER_BACKEND=online_store` で ranking が fake 経由で動作 |
| PR-3 | `VectorSearchWriter` Port + adapter (pipeline 層) + embed pipeline 二重書き | `pipeline/data_job/main.py` がローカルで BQ + fake VVS の両方に書く |
| PR-4 | KServe Reranker → Feature Online Store opt-in 参照 (Phase 7 固有) | `KSERVE_FEATURE_ONLINE_URL` 設定時のみ feature が VVS 経路で取得される |
| Wave 2 → | Terraform / IAM / deploy | 別 roadmap section §4 参照 |

### 2.3 default 切替の段取り

```
Step A: 全 PR merge 直後 → default = bq / inline (現状維持)
Step B: Wave 2 完了で GCP 側 index endpoint provision OK 後 → default = vertex_vector_search に flip
Step C: Step B が dev で 1 週間安定したら本 phase docs/01 §「実装段差」表を「Vertex Vector Search 以外撤去」に更新
```

### 2.4 ローカル完結スコープ (まず取りかかる範囲)

> **Wave 1 のコード変更はすべてローカル完結で書ける**。実 GCP 通信を伴う検証は Wave 2 で provision された後にまとめて行う。Wave 1 の受け入れ条件 (`make check` / `make check-layers` / unit test / in-memory fake 経由の `/search`) はすべて GCP 認証無しで成立する。

| PR | コード作業 | ローカル検証 | GCP 必要な部分 (Wave 2 で実施) |
|---|---|---|---|
| PR-1 SemanticSearch | adapter / settings / composition / fake / unit test の追加 — 全てローカル可 | `SEMANTIC_BACKEND=vertex_vector_search` + in-memory fake で `/search` 200 / mock で `find_neighbors` を呼ぶ unit test | live `aiplatform.MatchingEngineIndexEndpoint.find_neighbors` smoke (Wave 2 で `VERTEX_VECTOR_SEARCH_INDEX_ENDPOINT_ID` 投入後) |
| PR-2 FeatureFetcher | Port / 2 adapter / fake / `ranking.py` 改修 — 全てローカル可 | `FEATURE_FETCHER_BACKEND=online_store` + fake fetcher で ranking 動作 | live `FeatureOnlineStoreServiceClient.fetch_feature_values` smoke |
| PR-3 VectorSearchWriter | Port / 2 adapter / pipeline component 改修 — 全てローカル可 | `pipeline/data_job/main.py` を fake adapter (BQ も fake) で完走 | live `MatchingEngineIndex.upsert_datapoints` smoke + 初回 backfill |
| PR-4 KServe → FOS opt-in | adapter / settings / manifest env 追加 — 全てローカル可 (manifest apply は Wave 2) | unit test で `KSERVE_FEATURE_ONLINE_URL` 切替が分岐する確認 | live KServe pod での opt-in 経路 smoke |

**ローカル開発前提**:

- Python 3.12 + `uv sync` で全依存解決済 (`google-cloud-aiplatform` は `pyproject.toml` に既存)
- `gcloud auth application-default login` は **不要** (mock / fake で完結)
- Docker 不要 (Meilisearch / Vertex SDK 通信は fake で stub)
- `make check` (ruff / format / mypy / pytest) は WSL ローカルで完走する前提

**ローカル完結の境界線**:

- `app/services/adapters/vertex_vector_search_semantic_search.py` の中身で Vertex SDK を import するのは OK。**実通信せず**、unit test で SDK call を mock して PASS させる
- `tests/integration/parity/` 配下の "live GCP 比較" テストは Wave 2 まで `pytest -m 'not live_gcp'` でスキップ可能なよう marker を付与する
- 環境変数 default は **必ず現行挙動と一致** させる (default off の Strangler 原則)

---

## 3. Wave 1 — 検索アプリ層 (本 roadmap の主タスク)

### 3.1 PR-1: `SemanticSearch` Port — Vertex Vector Search adapter [ローカル完結 ✓]

**目的**: ME5 で encode したクエリベクトルを Vertex AI Vector Search の match endpoint に投げ、候補 (property_id + score) を返す経路を Port/Adapter で追加。BQ adapter は据え置き。**コードと unit/integration test までローカル完結。live GCP smoke は Wave 2。**

**ファイル一覧**:

| 操作 | パス | 役割 |
|---|---|---|
| 新規 | `app/services/adapters/vertex_vector_search_semantic_search.py` | `aiplatform.MatchingEngineIndexEndpoint.find_neighbors()` を叩く adapter |
| 維持 | `app/services/adapters/bigquery_semantic_search.py` | Phase 4 同等の挙動 (削除しない) |
| 維持 | `app/services/protocols/semantic_search.py` | Port 定義は不変 (interface 同形なら既存で吸収) |
| 編集 | `app/composition_root.py` (or `app/container/search.py`) | `SEMANTIC_BACKEND` 環境変数で adapter 注入分岐 |
| 編集 | `app/settings/settings.py` | `semantic_backend: Literal["bq", "vertex_vector_search"] = "bq"` を追加 (default は現行維持) |
| 新規 | `app/services/noop_adapters/in_memory_semantic_search.py` | テスト / ローカル開発用 fake (任意の固定 candidate を返す) |
| 新規 | `tests/unit/services/test_vertex_vector_search_semantic_search.py` | adapter 単体 (Vertex SDK は mock) |
| 新規 | `tests/integration/parity/test_semantic_backend_parity.py` | 同一クエリで BQ adapter と VVS adapter の上位 K が極端にズレないことを検証 (許容差は文書化) |
| 編集 | `scripts/ci/layers.py` の RULES | 新 adapter のレイヤ境界を追加 |

**設計メモ**:

- Vertex Vector Search の `find_neighbors` は `(neighbor_id, distance)` を返す。Port が要求する `SemanticResult(property_id, score, rank)` に合わせて変換する mapper をこの adapter 内に持つ
- `embedding_dim` は 768 (`ml/common/config/embedding.py`) で固定、index 作成時とランタイムで一致を保証
- 失敗時は `BigQuerySemanticSearch` への自動 fallback は **しない** (Strangler ではあくまで env 切替で挙動が決まる)
- 認証は ADC (Cloud Run / GKE の Workload Identity 経由)、ローカルは `gcloud auth application-default login`

**受け入れ条件**:

ローカル (PR merge 時に必須):
- [ ] `make check` PASS (ruff / format / mypy / pytest)
- [ ] `make check-layers` PASS (composition root のレイヤ境界)
- [ ] `SEMANTIC_BACKEND=vertex_vector_search` を `env/.env.example` に追記
- [ ] in-memory fake 経由で `/search` が 200 を返す (実 GCP 不要)
- [ ] mock で Vertex SDK call を stub した unit test PASS
- [ ] `tests/unit/app/test_api_contract_template.py` の出力 schema 不変

GCP smoke (Wave 2 で実施):
- [ ] live `MatchingEngineIndexEndpoint.find_neighbors` 経由で `/search` 200
- [ ] `tests/integration/parity/test_semantic_backend_parity.py` の live 比較 PASS

---

### 3.2 PR-2: `FeatureFetcher` Port — Feature Online Store adapter [ローカル完結 ✓]

**目的**: rerank 入力 feature を Feature Online Store から取得する経路を Port/Adapter として用意。training-serving skew 防止 (Phase 5 必須要素を Phase 7 でも維持)。

**ファイル一覧**:

| 操作 | パス | 役割 |
|---|---|---|
| 新規 | `app/services/protocols/feature_fetcher.py` | `class FeatureFetcher(Protocol): def fetch(self, property_ids: list[str]) -> dict[str, FeatureRow]` |
| 新規 | `app/services/adapters/feature_online_store_fetcher.py` | Vertex AI SDK `FeatureOnlineStoreServiceClient.fetch_feature_values` |
| 新規 | `app/services/adapters/bigquery_feature_fetcher.py` | 既存の BQ 直読挙動を Port 化したもの (default 維持用) |
| 新規 | `app/services/noop_adapters/in_memory_feature_fetcher.py` | fake (固定 feature を返す) |
| 編集 | `app/services/ranking.py` | feature 取得を直書きから `FeatureFetcher` Port 経由に変更 |
| 編集 | `app/composition_root.py` | `FEATURE_FETCHER_BACKEND=online_store|bq|fake` で adapter 注入 |
| 編集 | `app/settings/settings.py` | `feature_fetcher_backend: Literal["bq", "online_store"] = "bq"` (default 現行維持) |
| 新規 | `tests/unit/services/test_feature_online_store_fetcher.py` | adapter 単体 |
| 新規 | `tests/integration/parity/test_feature_fetcher_parity.py` | bq fetcher と online store fetcher の取得値が一致 (training-serving skew チェック) |

**受け入れ条件**:

ローカル (PR merge 時に必須):
- [ ] `make check` PASS
- [ ] `make check-layers` PASS (新 Port / adapter のレイヤ境界が RULES に追加されている)
- [ ] in-memory fake fetcher 経由で ranking が動作
- [ ] feature parity invariant 6 ファイル ([`pipeline/data_job/dataform/features/property_features_daily.sqlx`](../pipeline/data_job/dataform/features/property_features_daily.sqlx) etc.) は不変

GCP smoke (Wave 2 で実施):
- [ ] live `FeatureOnlineStoreServiceClient.fetch_feature_values` 経由で ranking 動作
- [ ] `tests/integration/parity/test_feature_fetcher_parity.py` の live skew check PASS

---

### 3.3 PR-3: `VectorSearchWriter` Port — embed pipeline の二重書き [ローカル完結 ✓]

**目的**: `embed_pipeline` が BQ embedding テーブルを書いた後、同じ embedding を Vertex Vector Search index に upsert する経路を追加。BQ は正本、Vertex Vector Search は serving index。

**ファイル一覧**:

| 操作 | パス | 役割 |
|---|---|---|
| 新規 | `pipeline/data_job/ports/vector_search_writer.py` | Port: `def upsert(rows: list[EmbeddingRow]) -> None` |
| 新規 | `pipeline/data_job/adapters/vertex_vector_search_writer.py` | `aiplatform.MatchingEngineIndex.upsert_datapoints` を呼ぶ adapter |
| 新規 | `pipeline/data_job/adapters/in_memory_vector_search_writer.py` | local fake |
| 編集 | `pipeline/data_job/components/` 内の embed コンポーネント | BQ MERGE の後段に upsert step を追加 (失敗しても BQ 側を巻き戻さない、観測可能性を持たせる) |
| 編集 | `pipeline/data_job/main.py` | DAG の wiring に upsert step を組み込み、`ENABLE_VECTOR_SEARCH_UPSERT` flag で skip 可能 |
| 新規 | `ml/data/loaders/vector_search_upserter.py` (任意) | low-level クライアント wrapper を ml 共通層に置く |
| 新規 | `tests/unit/pipeline/test_vector_search_writer.py` | fake で upsert が呼ばれることを検証 |

**設計メモ**:

- BQ MERGE と Vertex Vector Search upsert は **別トランザクション**。一方が失敗しても他方は完了する (eventual consistency 設計、観測可能性は Cloud Logging の構造化ログで担保)
- Vertex Vector Search index の build / refresh は本番では batch update を推奨。streaming update は cost が大きい
- 初回 backfill は別スクリプト (`scripts/setup/backfill_vector_search_index.py`) を Wave 2 で追加 — Wave 1 では DAG への組み込みのみ

**受け入れ条件**:

ローカル (PR merge 時に必須):
- [ ] `make check` PASS
- [ ] ローカルで `pipeline/data_job/main.py` を fake adapter (BQ writer + VVS writer 両方 fake) で完走
- [ ] `ENABLE_VECTOR_SEARCH_UPSERT=false` で skip され、現行挙動と同等
- [ ] mock で `MatchingEngineIndex.upsert_datapoints` を stub した unit test PASS

GCP smoke (Wave 2 で実施):
- [ ] live `MatchingEngineIndex.upsert_datapoints` で実 index に書き込み
- [ ] BQ MERGE と VVS upsert が同一 run で eventual に整合 (Cloud Logging 観測)

---

### 3.4 PR-4: KServe Reranker → Feature Online Store opt-in (Phase 7 固有) [ローカル完結 ✓ — manifest apply のみ Wave 2]

**目的**: KServe pod で動く reranker が、predict 直前に Feature Online Store から feature を引く経路を opt-in で追加。default off。Phase 7 docs/01 §「Phase 7 固有として KServe pod から Feature Online Store を opt-in 参照する経路を追加」に対応するコード実装。

**ファイル一覧**:

| 操作 | パス | 役割 |
|---|---|---|
| 編集 | `app/services/adapters/kserve_reranker.py` | predict 前に `FeatureFetcher` を opt-in で呼ぶ (env `KSERVE_FEATURE_ONLINE_URL` で制御) |
| 編集 | `app/composition_root.py` | KServe reranker に対して FeatureOnlineStoreFetcher を注入する分岐 |
| 編集 | `app/settings/settings.py` | `kserve_feature_online_url: str | None = None` を追加 |
| 編集 | `infra/manifests/kserve/reranker.yaml` (Wave 2 寄りだが env だけ Wave 1 で追加可) | env 変数の vehicle を用意 |
| 新規 | `tests/integration/test_kserve_reranker_with_online_store.py` | opt-in 有効 / 無効それぞれで分岐確認 |

**受け入れ条件**:

ローカル (PR merge 時に必須):
- [ ] `make check` / `make check-layers` PASS
- [ ] default (`KSERVE_FEATURE_ONLINE_URL` 未設定) で挙動変わらず (unit test で確認)
- [ ] 設定時のみ Feature Online Store の fetch 経路に分岐 (unit test で確認)
- [ ] `infra/manifests/kserve/reranker.yaml` の env vehicle 追加 (apply は Wave 2)

GCP smoke (Wave 2 で実施):
- [ ] live KServe pod に env 注入 → 実 Feature Online Store fetch 経路で `/search` 200
- [ ] env 未設定で従来挙動 (cluster 上で確認)

---

### 3.5 全 PR 共通: composition root + settings + env 取り扱い

| 環境変数 | 値域 | default | 影響 |
|---|---|---|---|
| `SEMANTIC_BACKEND` | `bq` / `vertex_vector_search` | `bq` | semantic 検索 adapter 切替 (PR-1) |
| `FEATURE_FETCHER_BACKEND` | `bq` / `online_store` | `bq` | rerank 用 feature 取得 adapter 切替 (PR-2) |
| `ENABLE_VECTOR_SEARCH_UPSERT` | `true` / `false` | `false` | embed pipeline の VVS upsert step を有効化 (PR-3) |
| `KSERVE_FEATURE_ONLINE_URL` | URL or 未設定 | 未設定 | KServe reranker の FOS opt-in 参照 (PR-4) |
| `VERTEX_VECTOR_SEARCH_INDEX_ENDPOINT_ID` | string | 未設定 | PR-1 / PR-3 で参照する Vertex Vector Search index endpoint ID (Wave 2 で provision) |
| `VERTEX_FEATURE_ONLINE_STORE_ID` | string | 既存 | 既に scripts/ops/vertex/feature_group.py が参照 |

`env/.env.example` と `infra/manifests/search-api/configmap.example.yaml` 双方を **同 PR で** 更新する (parity invariant)。

---

## 4. Wave 2 — GCP インフラ層 (Wave 1 完了後)

> **本 roadmap のスコープ外。具体は別 roadmap or PR で扱う。** ここでは何が必要かのチェックリストだけ載せる。

- [ ] `infra/terraform/modules/vector_search/` を実装 (`google_vertex_ai_index` + `google_vertex_ai_index_endpoint` + `google_vertex_ai_index_endpoint_deployed_index_resource`)
- [ ] `infra/terraform/environments/dev/main.tf` で `module "vector_search"` を有効化 + 出力 (index_endpoint_id) を IAM / runtime に伝搬
- [ ] `infra/terraform/modules/vertex/variables.tf::enable_feature_online_store` の default を `true` に (PDCA 都合で env override 可)
- [ ] `infra/terraform/modules/iam/main.tf` の `sa-api` / `sa-pipeline` に `roles/aiplatform.user` を付与 (Vector Search query / upsert 権限)
- [ ] KServe pod の SA に Vertex Vector Search query 権限を Workload Identity で bind
- [ ] `scripts/setup/backfill_vector_search_index.py` (初回 backfill 用 one-off)
- [ ] `make deploy-all` の wiring (vector_search module を tf apply 順序に組み込む)
- [ ] `scripts.deploy.monitor` に vector_search smoke step を追加
- [ ] `make run-all-core` に `ops-vertex-vector-search-smoke` を追加

---

## 5. Wave 3 — docs / reference architecture との整合 (確認のみ)

- [ ] 本 phase [docs/01_仕様と設計.md](01_仕様と設計.md) §「実案件想定の reference architecture」(Phase 5 docs を参照する旨) が最新であること
- [ ] コードに `Elasticsearch` / `synonym` / `query expansion` 等の固有名が混入していないことを `scripts/ci/layers.py` の禁止語リスト (or grep based check) で守る — 任意の追加チェック
- [ ] [docs/03_実装カタログ.md](03_実装カタログ.md) と [docs/05_運用.md](05_運用.md) の「semantic 経路」「feature 取得経路」の記述を Wave 1 / Wave 2 完了に追従して更新

---

## 6. リスクと回避

| リスク | 回避 |
|---|---|
| 大型 PR になりがち | 1 PR = 1 Port (PR-1 ～ PR-4) で 4 PR に分割。各 PR は default off で merge できる |
| default 挙動が壊れる | Strangler 原則: 既存 BQ adapter を残し env 切替、default は現行維持。Wave 1 merge では実際の挙動は変わらない |
| feature parity 6 ファイル更新漏れ | parity invariant の対象は 6 ファイル固定 (本 phase CLAUDE.md 参照)。SemanticSearch には影響しないが、FeatureFetcher 追加時は `ml/data/feature_engineering/` 周辺を要確認 |
| Vertex Vector Search index の build 時間 | Wave 2 で別ジョブ化。初回は手動 trigger、以後 `embed_pipeline` schedule にチェーン (PR-3 の upsert step が daily incremental 担当) |
| Feature Online Store のコスト | `enable_feature_online_store` の env 切替を Wave 2 で残す。学習用 dev では opt-out 可能、real serving 時に flip on |
| Vertex Vector Search match endpoint の権限不足で Wave 2 直後に 403 | sa-api / KServe SA に `aiplatform.user` を Workload Identity で bind (Wave 2 IAM チェックリスト) |
| BQ → VVS の embedding 不整合 | PR-3 の upsert は BQ MERGE と分離 (eventual consistency)。整合性は `tests/integration/parity/` で検証、観測は Cloud Logging |

---

## 7. マイルストーン

| ID | フェーズ | 完了内容 | 期待される証跡 |
|---|---|---|---|
| M1 | ローカル | PR-1 merge | `SEMANTIC_BACKEND=vertex_vector_search` で in-memory fake 経由 `/search` が 200、`make check` PASS |
| M2 | ローカル | PR-2 merge | `FEATURE_FETCHER_BACKEND=online_store` で fake 経由 ranking 動作、`make check` PASS |
| M3 | ローカル | PR-3 merge | embed pipeline がローカルで BQ fake + VVS fake の両方に書く、`make check` PASS |
| M4 | ローカル | PR-4 merge | `KSERVE_FEATURE_ONLINE_URL` 切替の unit test PASS、manifest env vehicle 追加 |
| **M-Local** | **ローカル完結** | **M1〜M4 全 merge** | **検索アプリ層が Wave 1 仕様で完成、GCP 抜きで `make check` PASS** |
| M5 | GCP | Wave 2 完了 | live GCP で `/search` が Vertex Vector Search 経由で動作、KServe pod で FOS opt-in 経路成立 |
| M6 | GCP | default flip | `SEMANTIC_BACKEND` default を `vertex_vector_search` に、本 phase docs/01 §4 表を更新 |
| M7 | docs | Wave 3 確認 | reference architecture 整合 docs check + コードに固有名混入なし |

---

## 8. 関連 docs

- 親リポ:
  - [README.md](../../../README.md) §1 教材対象外 / §3 非負制約 / §4 学習運用
  - [CLAUDE.md](../../../CLAUDE.md) §「非負制約 (Phase 3/4/5/6/7 共通)」
  - [docs/01_仕様と設計.md](../../../docs/01_仕様と設計.md) §「ハイブリッド検索の仕様と設計 (Phase 3-7 共通)」
- 本 phase:
  - [README.md](../README.md)
  - [CLAUDE.md](../CLAUDE.md)
  - [docs/01_仕様と設計.md](01_仕様と設計.md) §「実案件想定の reference architecture」(Phase 5 を継承)
  - [docs/02_移行ロードマップ-Port-Adapter-DI.md](02_移行ロードマップ-Port-Adapter-DI.md) Port / Adapter / DI 大枠
  - [docs/decisions/](decisions/README.md) 過去の制約決定 (ADR 0001〜0008)
- Phase 5 (継承元):
  - [5/study-hybrid-search-vertex/docs/01_仕様と設計.md](../../../5/study-hybrid-search-vertex/docs/01_仕様と設計.md)
  - [5/study-hybrid-search-vertex/docs/02_移行ロードマップ.md](../../../5/study-hybrid-search-vertex/docs/02_移行ロードマップ.md)
