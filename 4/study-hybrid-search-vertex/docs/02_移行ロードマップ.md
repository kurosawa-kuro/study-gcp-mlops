# 02_移行ロードマップ（未対応項目リスト）

本ドキュメントは、Phase 4 (`study-hybrid-search-vertex`) のうち **Vertex 実装で未対応の項目だけ**を管理する。
対応済みの履歴は記載しない。

---

## 1. 実 GCP 検証（最優先）

- [ ] `infra/terraform` の apply を実環境で実施し、Vertex 関連リソース作成を確認する
- [ ] `property-encoder-endpoint` の実デプロイ（Model Registry 登録 -> Endpoint 反映）を完了する
- [ ] `property-reranker-endpoint` の実デプロイ（Model Registry 登録 -> Endpoint 反映）を完了する
- [ ] `search-api` から 2 Endpoint を経由した `/search` を実疎通確認する

## 2. Pipeline 本番疎通

- [ ] `embed_pipeline` の submit -> 完走を確認する
- [ ] `train_pipeline` の submit -> 完走を確認する
- [ ] `train_pipeline` の評価ゲート（NDCG閾値）の実挙動を確認する
- [ ] Model Registry の staging/production エイリアス運用を実確認する

## 3. 監視・再学習トリガー

- [ ] Vertex Model Monitoring v2 の実アラート発火を確認する
- [ ] `model-monitoring-alerts` -> Eventarc -> Cloud Function -> Pipeline submit の連鎖を実確認する
- [ ] Scheduler 起点の再学習トリガーを実確認する

## 4. 運用・品質確認

- [ ] `make deploy-all` / `make destroy-all` の実 GCP PDCA を Phase 4 で再検証する
- [ ] 運用 Runbook（`docs/04_運用.md`）を実測値（所要時間・失敗ケース）で更新する
- [ ] コスト観測（BQ/Vertex/Cloud Run）を初回サイクルで記録する

## 5. 整理ルール

- [ ] 対応した項目はこのファイルから削除し、未対応のみを維持する
- [ ] 新しい未対応が発生した場合のみ本ファイルへ追記する

