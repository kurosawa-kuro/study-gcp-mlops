# 04_運用

## 前提

- Docker + Docker Compose
- Python 3.10+

## 基本フロー

```bash
make build
make seed
make train
make serve
make test
```

## 各ジョブの実体

- `make seed` -> `python -m pipeline.data_job.main`
- `make train` -> `python -m pipeline.training_job.main`
- `make serve` -> `uvicorn app.main:app`
- `make test` -> `pytest tests/`

## 生成物

- モデル: ml/registry/artifacts/<run_id>/model.lgb
- メトリクス: ml/registry/artifacts/<run_id>/metrics.json
- 最新モデル参照: ml/registry/artifacts/latest
- 予測結果: PostgreSQL `prediction_runs`

## クリーンアップ

```bash
make clean
```

削除対象:

- ml/registry/artifacts/

## ディレクトリ（現行）

- app/
  - api/
  - services/
  - schemas/
- common/
- ml/
  - core/
  - ports/
  - adapters/
- pipeline/
  - data_job/
  - training_job/
  - batch_serving_job/
- infra/
- docs/
- scripts/
- tests/
