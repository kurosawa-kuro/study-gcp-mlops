# 01_仕様と設計 (Phase 2: App + Pipeline + Port-Adapter)

> **canonical な Phase 横断仕様**は [`7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md`](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md) (Phase 7 = 到達ゴール / 引き算で後方派生)。
> **教育設計の正本**は親 [`README.md`](../../../README.md) (Phase 一覧 + 引き算戦略)。
> **本ファイル**は Phase 2 固有の仕様。Phase 1 (ML 基礎) との差分を示す。

---

## 0. 位置付け (引き算戦略上の Phase 2)

Phase 2 は親 [`README.md` §4](../../../README.md) の引き算チェーン (`Phase 7 → 6 → 5 → 4 → 3 → 2 → 1`) で **Phase 3 から不動産ハイブリッド検索一式 (Meilisearch / multilingual-e5 / LightGBM LambdaRank / Redis) を引き算した結果**。題材は Phase 1 から継承して California Housing (回帰) を維持しつつ、**App / Pipeline / Port-Adapter / DI** の設計パターンを学ぶフェーズ。

---

## 1. Phase 1 → Phase 2 の差分

| 観点 | Phase 1 | Phase 2 (追加) |
|---|---|---|
| 題材 | California Housing 回帰 | 同一 (不変) |
| 学習器 | LightGBM 回帰 | 同一 (不変) |
| 評価 | RMSE / R² | 同一 (不変) |
| API | なし | **`/predict` / `/health` (FastAPI)** |
| DI | なし | **lifespan DI** (`app/`, `ml/container.py`) |
| 設計パターン | flat module | **Port / Adapter** (`ml/ports/`, `ml/adapters/`) |
| job 構造 | data_job / training_job / evaluation_job | **+ batch_serving_job** (推論系) |
| データ層 | PostgreSQL 直接 | Filesystem / PostgreSQL adapter (Port 経由) |

**変えるのは**: API 入口・job 入口・依存方向・DI。
**変えないのは**: 題材と ML コア (LightGBM 回帰)。

---

## 2. 中核アーキテクチャ (`core → ports ← adapters`)

```
Inbound Adapter (FastAPI / job runner)
       ↓
   ml/core (純粋ロジック)
       ↓
   ml/ports (抽象 Protocol)
       ↑
   ml/adapters (PostgreSQL / Filesystem 実装)
       ↑
   ml/container.py (依存配線)
```

依存方向は **常に internal → external** (core は ports のみ知り、adapter は知らない)。この依存方向は **Phase 3 以降も不変**で、ハイブリッド検索ドメインに展開しても adapter だけ差し替えていく。

---

## 3. 主要コンポーネント

### 3.1 `app/` (FastAPI)

- `/predict` — 予測エンドポイント (Predictor 経由)
- `/health` — liveness
- `lifespan` — 起動時に Container を構築、DI 注入

### 3.2 `ml/core` (純粋ロジック)

- preprocess / feature engineering / trainer / evaluation

### 3.3 `ml/ports` / `ml/adapters` (抽象 ↔ 実装)

| Port | Adapter (production) |
|---|---|
| `DatasetReader` | `PostgresDatasetReader` |
| `ModelStore` | `FilesystemModelStore` |
| `Predictor` | `LightGBMPredictor` |

### 3.4 `pipeline/` (job 分離)

- `data_job` — sklearn → PostgreSQL 投入
- `training_job` — LightGBM 学習
- `batch_serving_job` — バッチ推論

### 3.5 `ml/container.py` (DI)

- 起動時に Adapter / Service を組み立て、`Container` (immutable) として返す
- handler は `Depends` で受け取る (`request.app.state.xxx` の `getattr` は使わない)

---

## 4. Phase 2 で扱わないもの (Phase 3 以降の責務)

- ハイブリッド検索 (Meilisearch / multilingual-e5 / RRF / LightGBM LambdaRank / Redis) → **Phase 3** で導入
- 不動産検索ドメイン → **Phase 3** で導入
- GCP マネージドサービス (Cloud Run / BigQuery / Pub/Sub / Secret Manager / Terraform / WIF) → **Phase 4** で導入
- Vertex AI 本番 MLOps 基盤 (Pipelines / Endpoint / Model Registry / Monitoring / **Feature Store** / **Vector Search**) → **Phase 5** で導入
- Cloud Composer / PMLE 追加技術 (BQML / Dataflow / TreeSHAP / SLO + burn-rate) → **Phase 6** で導入
- GKE / KServe → **Phase 7** で導入
- 全 Phase 教材対象外 (Agent Builder / Vizier / Model Garden / Gemini RAG / W&B / Looker Studio / Doppler) — 親 [`README.md` §1](../../../README.md)

---

## 5. 不変ルール

- 題材は California Housing 回帰 (Phase 1 から継承)
- ML コアは LightGBM 回帰
- Phase 2 は App / Pipeline / Port-Adapter を学ぶフェーズ — 検索ドメインや GCP 責務は持ち込まない
- 設計思想 (Port/Adapter / `core → ports ← adapters` / DI) は **Phase 3 以降も不変**で、本 phase が起点

---

## 6. 関連ドキュメント

- 本 phase: [`02_移行ロードマップ.md`](02_移行ロードマップ.md) (権威 1 位、決定的仕様) / [`03_実装カタログ.md`](03_実装カタログ.md) / [`04_運用.md`](04_運用.md) / [`TASKS.md`](TASKS.md) / [`教育資料/`](教育資料/)
- 親リポ: [`README.md`](../../../README.md) (Phase 一覧 + 引き算戦略 + Cloud Composer 位置づけ) / [`docs/architecture/01_仕様と設計.md`](../../../docs/architecture/01_仕様と設計.md) (薄いナビゲーションハブ)
- canonical (Phase 7): [`7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md`](../../../7/study-hybrid-search-gke/docs/architecture/01_仕様と設計.md) (Phase 横断仕様 §2 + Composer §3)
- 前 phase: [`1/study-ml-foundations/docs/01_仕様と設計.md`](../../../1/study-ml-foundations/docs/01_仕様と設計.md) (Phase 1 ML 基礎)
