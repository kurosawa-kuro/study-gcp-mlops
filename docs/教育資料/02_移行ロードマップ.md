# 02_移行ロードマップ

本ドキュメントは、`docs/教育資料/` 配下の教育資料を **Phase 1〜7 の最新仕様と現行コードに合わせて更新するための作業計画書** である。  
今回は **計画書の作成まで** を対象とし、教材本文の改稿やシナリオ作成は扱わない。

## 1. このドキュメントの位置づけ

- 対象ディレクトリ: `/home/ubuntu/repos/study-gcp-mlops/docs/教育資料`
- 対象 Phase: **Phase 1〜7**
- **Phase 8 (`8/study-hybrid-search-gitops/`) は今回のスコープ外**
- このファイルは **教育資料側の改修計画書** であり、以下の同名ファイルとは役割が異なる
  - `6/study-hybrid-search-pmle/docs/02_移行ロードマップ.md`
  - `7/study-hybrid-search-gke/docs/tasks/TASKS_ROADMAP.md`

混同防止のため、Phase 配下の `02_移行ロードマップ.md` は **各 Phase 自身の仕様・残課題管理**、本ファイルは **教育資料全体の調査と改修計画** を扱う。

## 2. 背景と目的

- 既存の教育資料が、最新の Phase 仕様およびコード実態と乖離している
- 学習者向け資料として、各 Phase で **何を新しく吸収してほしいか** を明確にしたい
- コード読解を主目的にせず、**新技術の理解と学習導線の再設計** を優先する
- 実作業の前段として、現状調査と改修タスクの棚卸しを先に行う

補足:
- `README.md` は Phase 1〜8 の全体像と非負制約の把握に使う
- Phase 1〜6 は、仕様書側の更新がコードより先行している可能性があるため、**各 Phase の `README.md` / `CLAUDE.md` / `docs/` と実コードの突合が必要**
- Phase 7 は最終 Phase として、比較的コードと仕様が一致している前提で確認を始める

## 3. 教育資料の作成方針

### 3.1 主眼

教育資料の目的は **新しい技術を吸収してもらうこと** であり、コード詳細の完全理解そのものではない。

### 3.2 記載してよい内容

- ML の重要要素の該当ファイル・該当行の抜粋
- モデル定義 / 学習 / 評価 / 推論の要点整理
- パイプラインと ML・推論の起動関係のフロー
- モデル格納場所や成果物配置の説明
- 精度評価の見方と確認ポイント
- サンプルアプリの挙動説明
- 必要に応じたコード全体の要約説明

教育資料では、次のような **具体テーマ** を扱ってよい。

- **ML 基礎機能**
  - `LightGBM` の役割、回帰 / ランキングでの使い分け
  - `scikit-learn` の位置づけ
    - Phase 1 では主にデータ取得や周辺用途
    - 学習器の主役は `LightGBM`
  - 前処理の基本
    - 欠損値補完
    - 外れ値処理
    - 対数変換
  - 特徴量エンジニアリングの基本
  - 学習・評価・推論の違い
  - `RMSE` / `R²` / `NDCG` など指標の読み方
  - モデル成果物、metrics、run 履歴の見方

- **アプリケーション / 設計パターン**
  - `FastAPI` の役割
  - `lifespan` による初期化
  - DI の考え方
  - `Port/Adapter` の依存方向
  - `Repository pattern`
  - `core -> ports <- adapters` の責務分離
  - job 分離
    - seed
    - train
    - batch serving
  - API と pipeline の役割の違い

- **検索 / ランキングの基本要素**
  - lexical search と semantic search の違い
  - `Meilisearch` の役割
    - `Elasticsearch` より導入しやすく、学習用ローカル構成として扱いやすいこと
  - `multilingual-e5` embedding の役割
  - `RRF` の役割
  - `LightGBM LambdaRank` の役割
  - cache (`Redis`) の使いどころ
  - `/search` のリクエストから再ランクまでの流れ

- **GCP 基礎機能**
  - `Cloud Run` の役割
  - `GCS` の役割
  - `BigQuery` の役割
  - `Cloud Logging` / `Cloud Monitoring` の役割
  - `Secret Manager` の役割
    - Meilisearch master key をどう扱うか
  - `Workload Identity Federation` の役割
  - `Terraform` によるリソース管理の考え方

- **Vertex AI 基礎機能**
  - `Vertex AI Pipelines` の役割
  - `Model Registry` の役割
  - `Endpoint` の役割
  - `Model Monitoring` の役割
  - Phase 4 の GCP 機能から、どこが Vertex 管理に置き換わるか

- **PMLE 追加技術の基礎要素**
  - `BQML`
  - `Dataflow`
  - `Monitoring SLO`
  - Explainable AI の基本的な見せ方
  - 「default 挙動は Phase 5 のまま、追加機能は opt-in」の考え方

- **GKE / KServe 基礎機能**
  - `GKE Autopilot` の役割
  - `KServe` の役割
  - `Gateway API` の役割
  - Kubernetes 上での serving 差し替えの考え方
  - Phase 6 から継承するものと、Phase 7 で差し替わるものの整理

- **再現性 / 成果物管理**
  - `run_id` の意味
  - モデル保存先と latest の見方
  - `metrics.json` や評価レポートの追い方
  - ローカル保存から GCS / Vertex 管理への移行
  - Phase 間で成果物を共有しない理由

- **テスト / 品質保証**
  - `pytest` の役割
  - unit test / integration test の違い
  - `make test` と `make check` の違い
  - lint / format / type check の意味
  - 「default 挙動を壊していない」ことをどう確認するか

- **設定 / シークレット管理**
  - `pydantic-settings` の役割
  - `setting.yaml` と `credential.yaml` の分離
  - 環境変数優先の考え方
  - ローカル設定とクラウド設定の違い
  - Secret をコードに埋め込まない理由

- **運用 / 監視の基礎**
  - ログを見る場所
  - metrics をどこで確認するか
  - 学習ジョブ / 推論サービス / バッチ処理の監視観点
  - `Cloud Monitoring` と `SLO` の違い
  - 障害時にどこから調べるかの導線

- **コスト / リソース意識**
  - ローカル実行とマネージドサービスのコスト差
  - 常時課金されやすい構成要素
  - 学習目的で一時的に有効化する機能と常設機能の違い

- **学習比較に使ってよい切り口**
  - 前 Phase と比べて増えた技術
  - 置き換わった adapter / service
  - 変わらない中核ロジック
  - ローカル実装からクラウド実装への責務移動
  - 「学ぶ対象」と「詳細説明を省いてよい対象」の線引き

- **学習者向けに補助してよい観点**
  - 各 Phase で新しく増えるコンポーネントの一覧
  - 前 Phase から何が増え、何が変わらないかの比較
  - ローカル実行とクラウド実行の違い
  - 実行コマンドと成果物確認ポイント
  - 「この Phase で理解すべきこと / 深追いしなくてよいこと」の切り分け

### 3.3 図表方針

- フロー図・関係図は **まず Mermaid で作る**
- SVG 化はレビュー段階で別途判断する

## 4. 参照順序

教育資料の改修時は、以下の優先順位で参照する。

1. 対象 Phase 配下の `README.md` / `CLAUDE.md` / `docs/`
2. ルート `README.md`
3. ルート `docs/` 配下の横断ハブ

主な横断参照先:

- `README.md`
- `docs/README.md`
- `docs/architecture/01_仕様と設計.md`
- `docs/architecture/03_実装カタログ.md`
- `docs/runbook/04_運用.md`
- `docs/phases/README.md`
- `docs/phases/phaseN/README.md`
- `docs/phase調整案.md`
- `docs/archive/README.md`

教育資料は既存 docs の複製ではなく、**学習者向けに再構成された別軸の成果物** として扱う。ただし、用語や仕様説明が既存 docs と矛盾しないことを必須条件とする。

## 5. Phase別の学習主眼

| Phase | テーマ | 教育資料で吸収させる新技術の主眼 |
|---|---|---|
| 1 | ML基礎（回帰） | LightGBM / preprocess / 評価 / artifact 出力 |
| 2 | App + Pipeline + Port/Adapter | FastAPI lifespan DI / `core -> ports <- adapters` / job 分離 |
| 3 | ハイブリッド検索（Local） | Meilisearch / multilingual-e5 / LambdaRank / Redis |
| 4 | ハイブリッド検索（GCP） | Cloud Run / GCS / BigQuery / Secret Manager / Terraform / WIF |
| 5 | Vertex AI 標準 MLOps | Vertex Pipelines / Endpoint / Model Registry / Monitoring |
| 6 | PMLE追加技術ラボ | BQML / Dataflow / SLO / Explainable AI ほか |
| 7 | GKE/KServe差分（Optional/Draft） | GKE Autopilot / KServe / Gateway API / Vertex CPR explain |

## 6. 横断的な不変ルール

教材の説明、図、比較表は以下の非負制約から逸脱しないこと。

- Phase 3〜7 は **LightGBM + multilingual-e5 + Meilisearch** のハイブリッド構成を必須とする
- Phase 6 は `/search` の default 挙動を **Phase 5 と一致** させ、新技術は opt-in で扱う
- Phase 7 は Phase 6 の学習基盤・データ基盤を継承し、**serving 層のみ差し替える**
- 教材対象外技術は親 [`README.md` §1 教材対象外](../../README.md) / [`docs/tasks/02_移行ロードマップ.md` §1.4](../tasks/02_移行ロードマップ.md) を参照
- `Secret Manager` は **Phase 4 の必須習得項目**

## 7. 現状調査で確認する論点

### 7.1 既存教育資料側

- `docs/教育資料/` 配下の既存ファイル一覧
- 各ファイルの役割
- 既に古くなっている説明
- 削除候補の記述
- 再利用可能な図表や章立て

### 7.2 Phase 正本側

- 各 Phase の `README.md` / `CLAUDE.md` / `docs/` が示す最新仕様
- 実コードが仕様に追随している範囲
- 教材で強調すべき学習対象
- 初学者向け説明に落とし込む際の難所

### 7.3 重点確認項目

- 教材対象外技術 (親 [`README.md` §1 教材対象外](../../README.md) / [`docs/tasks/02_移行ロードマップ.md` §1.4](../tasks/02_移行ロードマップ.md) 参照) の残存有無
- `Secret Manager` の扱いが Phase 4 で十分に説明されているか
- Phase 6 の PMLE 拡張技術が、default 挙動維持の前提付きで説明されているか
- Phase 7 が「Phase 6 の全面置換」ではなく「serving 差し替え」で説明されているか

## 8. 差分洗い出しの観点

教育資料の改修対象は、少なくとも以下の観点で分類する。

- **削除**: 既に採用しない技術、古い構成、誤った説明
- **更新**: 用語、構成図、実行手順、成果物配置、アーキテクチャ説明
- **追加**: 新技術の説明、図、比較、学習順序、注意点
- **保留**: Phase 正本とコードの差分確認後に判断すべき項目

特に Phase 1〜6 は、仕様更新が先行している可能性を前提に、**「README に書かれている」ことだけで教材内容を確定しない**。必ず Phase 配下資料と実コードの突合結果を残す。

## 9. Phase別の調査・修正タスク

### Phase 1

- ML 基礎教材として不要なアプリ説明が混入していないか確認
- preprocess / 学習 / 評価 / artifact 出力の導線を整理
- `runs/{run_id}/` など成果物の見せ方を確認

### Phase 2

- FastAPI / DI / Port/Adapter / job 分離の説明が最新構成と一致するか確認
- Phase 1 との差分が学習者に明確に伝わる構成にする

### Phase 3

- ローカルハイブリッド検索の全体像を、lexical / semantic / rerank の3段で説明できているか確認
- Meilisearch / multilingual-e5 / LambdaRank / Redis の役割分担を明確化

### Phase 4

- GCP 移行で増える管理対象を整理
- `Secret Manager` を必須学習項目として明示
- Cloud Run / BigQuery / GCS / Terraform / WIF の接続関係を図示できる形にする

### Phase 5

- Vertex AI への差し替えポイントを Phase 4 比較で説明
- Endpoint / Pipelines / Model Registry / Monitoring の役割分担を整理

### Phase 6

- PMLE の追加技術を「Phase 5 default を壊さず opt-in で学ぶ」前提で整理
- BQML / Dataflow / SLO / Explainable AI などを、本流と副経路の区別つきで説明

### Phase 7

- Phase 6 からの継承部分と、GKE/KServe による serving 差分を分離して説明
- `serving 層のみ差し替え` を崩す説明になっていないか確認

## 10. 実施手順

1. `docs/教育資料/` 配下の既存資料を棚卸しする
2. Phase 1〜7 の `README.md` / `CLAUDE.md` / `docs/` を確認する
3. 必要箇所は実コードまで降りて、仕様とコードの差分を記録する
4. 教育資料の既存内容を `削除 / 更新 / 追加 / 保留` に分類する
5. Phase 別の修正タスクを一覧化する
6. 図表化が必要な箇所を Mermaid 前提で洗い出す
7. 教育資料の改修順序を決める

## 11. 成果物

今回の成果物は次の 1 点とする。

- `docs/教育資料/02_移行ロードマップ.md`
  - 既存資料の現状調査方針
  - Phase別の確認観点
  - 修正タスクの洗い出し方針
  - 改修時の不変ルール

今回は **シナリオ作成を含めない**。教材本文や図版の新規作成は、この計画書に基づく後続タスクとする。
