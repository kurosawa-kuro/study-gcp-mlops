# ローカル動作検証結果 (Phase 1 / study-ml-foundations)

実施日: 2026-04-22
対象: `1/study-ml-foundations/`
順序: ML (build → seed → train) → app (serve) → test

## 事前準備: 競合ポート自動解放の仕組みを追加

host:5432 / host:8000 を他プロジェクトの docker コンテナ（例: `real-estate-postgres`）
が掴んでいると Phase 1 の postgres/api が起動できず、毎回手動で `docker stop` する
必要があった。検証速度を上げるため、`make` 経由で自動停止する仕組みを入れた。

- `scripts/core.py`:
  - 定数 `HOST_PORTS = (5432, 8000)` と `OWN_CONTAINER_PREFIX = "ml-pipeline-"` を追加
  - `free_host_ports()`: 指定ポートを publish している非プロジェクトコンテナを `docker stop`
  - `compose()` を拡張: `up` / `run` サブコマンド時に自動呼び出し
- `Makefile`: 標準ターゲット `free-ports` を追加（単体実行も可能）

これで `make seed` / `make train` / `make serve` は実行前に競合ポートを無条件で解放する。

## 検証手順と結果

### 1. ML パイプライン

| ステップ | 結果 | 備考 |
|---|---|---|
| `make build` | ✅ PASS | 初回 `api` ビルドで `COPY common/ common/` が解決不能だったため、`infra/run/services/api/Dockerfile` から当該行を削除（`common/` は `ml/common/` に移動済みのため不要）。修正後は 3 イメージ（api / seed / trainer）全てビルド成功 |
| `make seed` | ✅ PASS | California Housing を sklearn から取得し PostgreSQL に投入。train=16,512 行 / test=4,128 行 |
| `make train` | ✅ PASS | LightGBM 300 iter。**RMSE=0.4663 / R²=0.8385**。Run ID `20260422_011119_3253e6`、`ml/registry/artifacts/latest → 20260422_011119_3253e6` symlink 更新、PostgreSQL に記録、W&B は offline モード |

### 2. API

`make serve` で FastAPI (Uvicorn) を起動。`app.state.booster` に `ml/registry/artifacts/latest/model.lgb` がロードされた。

| エンドポイント | HTTP | レスポンス |
|---|---|---|
| `GET /health` | 200 | `{"status":"ok"}` |
| `POST /predict` | 200 | `{"predicted_price":4.1679}` （サンプル入力: `MedInc=8.3252, HouseAge=41, ...`） |
| `GET /metrics` | 200 | HTML（Jinja2 テンプレート表示） |
| `GET /data` | 200 | HTML（Jinja2 テンプレート表示） |

### 3. pytest (`make test`)

初回は `httpx` と `testcontainers[postgres]` が dev 依存に未登録で collection error。
`pyproject.toml` の `dependency-groups.dev` に追加して `uv sync` 後、再実行。

**結果: 22 passed / 0 failed** (約 31 秒)

- `tests/integration/test_api.py` (2): /health, /predict
- `tests/integration/test_pipeline.py` (6): Settings, PostgresRepository (testcontainers 経由)
- `tests/unit/ml/test_evaluation.py` (8): RMSE/R²/save_metrics/wandb offline
- `tests/unit/ml/test_preprocess.py` (5): 欠損補完/外れ値キャップ/対数変換
- `tests/unit/ml/test_trainer.py` (1): LightGBM 学習 + 保存

## 今回の変更一覧

- `infra/run/services/api/Dockerfile` — `COPY common/ common/` を削除（ソースツリー側で `ml/common/` に移動済みだったがリネーム漏れ）
- `scripts/core.py` — `free_host_ports()` 追加、`compose()` で自動呼び出し
- `Makefile` — `free-ports` ターゲット追加、`.PHONY` に登録
- `pyproject.toml` — dev group に `httpx` / `testcontainers[postgres]` 追加

---

# ローカル動作検証結果 (Phase 2 / study-ml-app-pipeline, Phase 3 / study-hybrid-search-local)

実施日: 2026-04-22
対象:
- `2/study-ml-app-pipeline/`
- `3/study-hybrid-search-local/`

## 背景

Phase 3 の `make verify-pipeline` は Docker サービス起動と host port (`8000`, `5432`, `5050`, `7700`, `6379`) を使用するため、
Phase 2 の `make serve` 稼働中に実行すると衝突リスクがある。

このため、`verify-pipeline` 単体で競合解消から検証まで自己完結できるように Make ターゲットを更新した。

## 変更内容（Phase 3）

変更ファイル: `3/study-hybrid-search-local/Makefile`

- `verify-pipeline` を以下の順で実行するよう更新
  1. `make up-clean`（`ports-free` で競合コンテナを停止してから compose 起動）
  2. `make ops-bootstrap`（migration + seed + sync + embed + train 準備）
  3. 既存の検証フロー（`check-layers`, `ops-*`, `eval-*`）

これにより、従来必要だった手動手順
(`phase2 停止 -> phase3 up -> phase3 ops-bootstrap -> phase3 verify-pipeline`) を
`make verify-pipeline` に集約した。

## 検証手順と結果

### Phase 2 (`2/study-ml-app-pipeline`)

| ステップ | 結果 | 備考 |
|---|---|---|
| `make test` | ✅ PASS | **16 passed, 3 skipped** |
| `curl http://localhost:8000/health` | ✅ PASS | `{"status":"ok"}` |

補足: 既存ターミナルの `make serve` は正常起動しており、health check は 200 を返却した。

### Phase 3 (`3/study-hybrid-search-local`)

| ステップ | 結果 | 備考 |
|---|---|---|
| `make test` | ✅ PASS | **51 passed, 0 failed** |
| `make check-layers` | ✅ PASS | `[check-layers] OK (stage=5)` |
| `make -n verify-pipeline` | ✅ PASS | 期待順で `up-clean -> ops-bootstrap -> 検証群` が展開されることを確認 |

## 今回の変更一覧（追記分）

- `3/study-hybrid-search-local/Makefile`
  - `verify-pipeline` ターゲット先頭に `up-clean` と `ops-bootstrap` を追加
  - ターゲット説明を「競合自動解消あり」に更新
