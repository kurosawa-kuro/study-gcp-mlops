# ローカル動作検証結果 (Phase 1 / study-ml-foundations)

実施日: 2026-04-22
対象: `1/study-ml-foundations/`
順序: ML (build → seed → train) → app (serve) → test

## 事前準備: 競合ポート自動解放の仕組みを追加

host:5432 / host:8000 を他プロジェクトの docker コンテナ（例: `real-estate-postgres`）
が掴んでいると Phase 1 の postgres/api が起動できず、毎回手動で `docker stop` する
必要があった。検証速度を上げるため、`make` 経由で自動停止する仕組みを入れた。

- `scripts/core.py`:
  - 定数 `HOST_PORTS = (5432, 8000)` と `OWN_CONTAINER_PREFIX = "ml-pipeline-"` を追加
  - `free_host_ports()`: 指定ポートを publish している非プロジェクトコンテナを `docker stop`
  - `compose()` を拡張: `up` / `run` サブコマンド時に自動呼び出し
- `Makefile`: 標準ターゲット `free-ports` を追加（単体実行も可能）

これで `make seed` / `make train` / `make serve` は実行前に競合ポートを無条件で解放する。

## 検証手順と結果

### 1. ML パイプライン

| ステップ | 結果 | 備考 |
|---|---|---|
| `make build` | ✅ PASS | 初回 `api` ビルドで `COPY common/ common/` が解決不能だったため、`infra/run/services/api/Dockerfile` から当該行を削除（`common/` は `ml/common/` に移動済みのため不要）。修正後は 3 イメージ（api / seed / trainer）全てビルド成功 |
| `make seed` | ✅ PASS | California Housing を sklearn から取得し PostgreSQL に投入。train=16,512 行 / test=4,128 行 |
| `make train` | ✅ PASS | LightGBM 300 iter。**RMSE=0.4663 / R²=0.8385**。Run ID `20260422_011119_3253e6`、`ml/registry/artifacts/latest → 20260422_011119_3253e6` symlink 更新、PostgreSQL に記録、W&B は offline モード |

### 2. API

`make serve` で FastAPI (Uvicorn) を起動。`app.state.booster` に `ml/registry/artifacts/latest/model.lgb` がロードされた。

| エンドポイント | HTTP | レスポンス |
|---|---|---|
| `GET /health` | 200 | `{"status":"ok"}` |
| `POST /predict` | 200 | `{"predicted_price":4.1679}` （サンプル入力: `MedInc=8.3252, HouseAge=41, ...`） |
| `GET /metrics` | 200 | HTML（Jinja2 テンプレート表示） |
| `GET /data` | 200 | HTML（Jinja2 テンプレート表示） |

### 3. pytest (`make test`)

初回は `httpx` と `testcontainers[postgres]` が dev 依存に未登録で collection error。
`pyproject.toml` の `dependency-groups.dev` に追加して `uv sync` 後、再実行。

**結果: 22 passed / 0 failed** (約 31 秒)

- `tests/integration/test_api.py` (2): /health, /predict
- `tests/integration/test_pipeline.py` (6): Settings, PostgresRepository (testcontainers 経由)
- `tests/unit/ml/test_evaluation.py` (8): RMSE/R²/save_metrics/wandb offline
- `tests/unit/ml/test_preprocess.py` (5): 欠損補完/外れ値キャップ/対数変換
- `tests/unit/ml/test_trainer.py` (1): LightGBM 学習 + 保存

## 今回の変更一覧

- `infra/run/services/api/Dockerfile` — `COPY common/ common/` を削除（ソースツリー側で `ml/common/` に移動済みだったがリネーム漏れ）
- `scripts/core.py` — `free_host_ports()` 追加、`compose()` で自動呼び出し
- `Makefile` — `free-ports` ターゲット追加、`.PHONY` に登録
- `pyproject.toml` — dev group に `httpx` / `testcontainers[postgres]` 追加

---

# ローカル動作検証結果 (Phase 2 / study-ml-app-pipeline, Phase 3 / study-hybrid-search-local)

実施日: 2026-04-22
対象:
- `2/study-ml-app-pipeline/`
- `3/study-hybrid-search-local/`

## 背景

Phase 3 の `make verify-pipeline` は Docker サービス起動と host port (`8000`, `5432`, `5050`, `7700`, `6379`) を使用するため、
Phase 2 の `make serve` 稼働中に実行すると衝突リスクがある。

このため、`verify-pipeline` 単体で競合解消から検証まで自己完結できるように Make ターゲットを更新した。

## 変更内容（Phase 3）

変更ファイル: `3/study-hybrid-search-local/Makefile`

- `verify-pipeline` を以下の順で実行するよう更新
  1. `make up-clean`（`ports-free` で競合コンテナを停止してから compose 起動）
  2. `make ops-bootstrap`（migration + seed + sync + embed + train 準備）
  3. 既存の検証フロー（`check-layers`, `ops-*`, `eval-*`）

これにより、従来必要だった手動手順
(`phase2 停止 -> phase3 up -> phase3 ops-bootstrap -> phase3 verify-pipeline`) を
`make verify-pipeline` に集約した。

## 検証手順と結果

### Phase 2 (`2/study-ml-app-pipeline`)

| ステップ | 結果 | 備考 |
|---|---|---|
| `make test` | ✅ PASS | **16 passed, 3 skipped** |
| `curl http://localhost:8000/health` | ✅ PASS | `{"status":"ok"}` |

補足: 既存ターミナルの `make serve` は正常起動しており、health check は 200 を返却した。

### Phase 3 (`3/study-hybrid-search-local`)

| ステップ | 結果 | 備考 |
|---|---|---|
| `make test` | ✅ PASS | **51 passed, 0 failed** |
| `make check-layers` | ✅ PASS | `[check-layers] OK (stage=5)` |
| `make -n verify-pipeline` | ✅ PASS | 期待順で `up-clean -> ops-bootstrap -> 検証群` が展開されることを確認 |

## 今回の変更一覧（追記分）

- `3/study-hybrid-search-local/Makefile`
  - `verify-pipeline` ターゲット先頭に `up-clean` と `ops-bootstrap` を追加
  - ターゲット説明を「競合自動解消あり」に更新

---

# ローカル動作検証結果 (Phase 4 / study-hybrid-search-cloud)

実施日: 2026-04-22  
対象: `4/study-hybrid-search-cloud/`

## 検証サマリ（確定分）

| 区分 | コマンド/項目 | 結果 | 備考 |
|---|---|---|---|
| 成功 | `make sync` | ✅ PASS | 依存同期完了（`.venv` 作成） |
| 成功 | `make lint` / `make fmt-check` | ✅ PASS | `ruff check` / format check 通過 |
| 成功 | `make test` | ✅ PASS | **195 passed** |
| 成功 | `make tf-validate` | ✅ PASS | Terraform validate 成功 |
| 成功 | `make train-smoke` | ✅ PASS | `ml.training.job --dry-run` 完走 |
| 失敗 | `make deploy-all` | ❌ FAIL | Step 7 (`deploy-api-local`) の Cloud Build が `docker build` 失敗 |
| 失敗 | `make run-all` | ❌ FAIL | `ops-feedback` で JSONDecodeError（API 応答が想定 JSON ではなく HTML） |

## 失敗詳細（確定）

### 1) `make deploy-all` 失敗

- 失敗ビルド: `bd0f20ef-4fe7-4826-b8f2-ec9a49a66792`
- 直接原因: `infra/run/services/search_api/Dockerfile` で存在しないファイルを `COPY`
  - `ml/embed/pyproject.toml`
  - `ml/train/pyproject.toml`
  - `ml/sync/pyproject.toml`

### 2) `make run-all` 失敗

- `check-layers`, `seed-test` は成功
- `ops-livez`, `ops-search` で Cloud Run の HTML (`Congratulations | Cloud Run`) を受信
- `ops-feedback` で `json.loads(...)` が失敗し停止（`JSONDecodeError`）

## 修正・是正（適用済）

- `infra/run/services/search_api/Dockerfile`
  - 不要かつ存在しない `ml/*/pyproject.toml` の `COPY` を削除
- デプロイ順の是正（モデル先行）
  - `scripts/dev/deploy_all.py` を **training-job → search-api** の順に変更
  - 仕様/運用ドキュメントに同順序を追記
    - `4/study-hybrid-search-cloud/docs/01_仕様と設計.md`
    - `4/study-hybrid-search-cloud/docs/03_実装カタログ.md`
    - `4/study-hybrid-search-cloud/docs/04_運用.md`

## 進捗更新（2026-04-22 追記）

### 1) training-job 側（モデル）

- `deploy-training-job-local` は成功済み
  - Cloud Build: `b98d4d58-f6dc-48fb-be2f-498c0c43f855`（`SUCCESS`, 約1分52秒）
  - Cloud Run Job `training-job`: `Ready=True`
  - 反映 image: `asia-northeast1-docker.pkg.dev/mlops-dev-a/mlops/training-job:2ac16e26`

### 2) search-api 側（API）

- 直近履歴:
  - `3756590c-21a1-4ea2-9f90-6ff567a352b8` は fail-fast 480 秒で `CANCELLED`
  - `9079a637-7f0a-43e0-ac69-b1acda0fdd2c` は確認時点で `WORKING`
- Cloud Run Service `search-api` の現行 revision はまだ placeholder
  - `latestReadyRevisionName: search-api-00001-wcc`
  - image: `gcr.io/cloudrun/hello`
  - traffic: 100% が上記 revision

### 3) 手順仕様の固定（適用済）

- `training-job -> search-api` の順序をコード・仕様書で固定
- `make deploy-all` は以下の順に変更済み:
  1. `deploy-training-job-local`
  2. `run-training-job-local`（モデル成立確認）
  3. `deploy-api-local`
- `deploy-api-local` にモデル先行ゲートを追加:
  - `mlops.training_runs` に `finished_at IS NOT NULL` が 1 件も無い場合は即失敗

## 進捗更新（2026-04-22 追加追記: API 再デプロイの確定結果）

### 1) API デプロイ結果（確定）

- `make deploy-api-local` は **成功**
  - revision: `search-api-00005-qz4`
  - image: `asia-northeast1-docker.pkg.dev/mlops-dev-a/mlops/search-api:2ac16e26-1776850929`
  - traffic: 100%
- その後 `make ops-enable-search` も **成功**
  - revision: `search-api-00006-9gr`
  - `ENABLE_SEARCH=true` を反映

### 2) 失敗/未達（現時点）

- `make ops-livez` は成功（`{"status":"ok"}`）
- `make ops-search` は HTTP 200 を返すが、**`results: []`（空配列）で実質未達**
  - 期待: 検索結果が1件以上返ること
  - 現状: API 起動・疎通は成功、ただし検索データ経路（候補データ/インデックス/埋め込み）のいずれかが未充足

### 3) 今回適用した恒久修正（再発防止）

- `delete-then-push` 廃止（タグ欠落レース回避）
- immutable tag (`<sha>-<epoch>`) 運用へ移行
- `search-api` Cloud Build timeout を実測ベースで `900s` に調整
- Cloud Run 起動失敗 (`ModuleNotFoundError: app`) 対策として search-api Docker runtime に:
  - `COPY app /home/app/app`
  - `PYTHONPATH=/home/app/app`

## 進捗更新（2026-04-22 追加追記: 動作検証の再開結果）

### 1) 個別検証の実行結果

- `make ops-livez` は **成功**
  - `{"status":"ok"}` を確認
- `make ops-search` は **失敗（未解消）**
  - HTTP 200 だが `results` が空配列で停止（既知症状を再確認）
- `make seed-test` の再実行で BigQuery 側の投入は成功
  - `properties_cleaned=5 / features_today=5 / embeddings=5`
  - ただし Meilisearch 同期で停止（後述）

### 2) 今回の修正（適用済）

- `4/study-hybrid-search-cloud/scripts/dev/seed_minimal.py`
  - `properties_cleaned` シード定義に `description` カラムを追加
  - `ml.data.loaders.meili_sync` が参照するカラム不整合（`Unrecognized name: description`）を解消

### 3) 現在のブロッカー（新規）

- `make seed-test` の Meilisearch 同期で ID トークン取得に失敗
  - エラー: `Neither metadata server or valid service account credentials are found.`
  - 追加確認: `gcloud auth print-identity-token --audiences=<meili-url>` は
    `Invalid account type ... Requires valid service account.` で失敗
- 影響: lexical 候補投入が完了せず、`ops-search` の空配列状態が継続

### 4) 実行方針の判断（deploy-all か個別か）

- **現時点は deploy-all 再実行より個別調整を優先**する
  - 理由: デプロイ自体より先に、検証データ投入経路（seed-test -> meili_sync）の認証前提が未充足
  - まずは認証経路（実行SA/impersonation/ID token発行）を成立させ、`seed-test -> ops-search -> ops-feedback` の順で再検証する
- 認証経路が復旧し、個別検証が通った段階で `make deploy-all` / `make run-all` を再実施する

---

# 動作検証予定 (Phase 4 / Phase 5)

実施予定日: TBD
対象:
- `4/study-hybrid-search-cloud/`
- `5/study-hybrid-search-vertex/`

## 方針

- Phase 3 で確立したハイブリッド検索要件（LightGBM + multilingual-e5 + Meilisearch 相当の責務）を維持したまま、
  実行基盤差分（Local -> GCP -> Vertex）を検証する
- まず「疎通と実行成功」を確認し、次に「品質・運用メトリクス」の妥当性を確認する
- 可能な項目は `make` ターゲット化し、再現可能な検証手順として固定する

## Phase 4 検証項目（予定）

### 1. 環境・認証

- [ ] `uv sync --dev` が成功する
- [ ] GCP 認証（WIF / service account impersonation）の前提チェックが通る
- [ ] 必須環境変数・シークレット解決（Secret Manager 参照）が成功する

### 2. データ・パイプライン

- [ ] BigQuery dataset / table の作成・参照が成功する
- [ ] Dataform compile/run（または同等ジョブ）が成功する
- [ ] 日次バッチ（feature 集計、評価集計）が完走する

### 3. 推論・検索 API

- [ ] Cloud Run service の deploy が成功する
- [ ] `/health` が 200 を返す
- [ ] `/search` の lexical + semantic + rerank が成立する
- [ ] `/feedback` が学習ログ蓄積に反映される

### 4. 学習・評価

- [ ] 再学習ジョブ（LightGBM LambdaRank）が完走する
- [ ] offline 評価（NDCG / MAP / Recall）を出力できる
- [ ] compare レポート（baseline vs rerank）を出力できる

### 5. 運用・監視

- [ ] 定常ジョブ（日次/週次）の実行手順を再現できる
- [ ] 失敗時リトライ・アラート導線を確認できる
- [ ] コストと実行時間の最小記録を残せる

## Phase 5 検証項目（予定）

### 1. Vertex AI 置換差分

- [ ] 学習パイプラインを Vertex Pipelines で実行できる
- [ ] 学習済みモデルを Vertex Model Registry に登録できる
- [ ] Endpoint デプロイ（online prediction）まで完了する

### 2. オンライン推論・検索統合

- [ ] 検索 API から Endpoint 推論呼び出しが成功する
- [ ] レイテンシ SLO（暫定値）を満たす
- [ ] fallback 経路（前モデル/簡易スコア）が動作する

### 3. 評価・改善ループ

- [ ] バッチ評価結果を次回再学習に接続できる
- [ ] モデル比較（champion/challenger）の判定基準を適用できる
- [ ] 必要時にロールバックできる

### 4. 監視・運用

- [ ] Endpoint のエラー率/遅延/スループット監視が有効
- [ ] データドリフト/品質監視の最小アラートを確認
- [ ] 週次運用 Runbook に沿って一連作業を再現できる

## 実施時に記録する結果テンプレート

各項目について以下を残す:

- 実施コマンド（または Job 名）
- 実行結果（PASS / FAIL）
- 主要メトリクス（時間、件数、品質指標）
- 失敗時の原因と対処

## 進捗更新（2026-04-22 追加追記: 認証経路修正と run-all 完了）

### 1) 今回のブロッカー切り分け結果

- `ops-search` 空配列の直接原因は **search-api から meili-search に到達できても検索実体が未稼働**だったこと
  - `meili-search` が `gcr.io/cloudrun/hello` のまま稼働していたため、Meilisearch API 応答になっていなかった
- Meilisearch 本体デプロイ後、次のブロッカーとして `seed-test` で `401 Unauthorized`
  - Cloud Run IAM 用 `Authorization: Bearer ...` と Meilisearch アプリ側認証の衝突が原因
- さらに `meili_sync` が `202 Accepted` の非同期完了前に後続検証へ進み、反映前検索で空配列化

### 2) 実施した修正

- Cloud Run `meili-search` を Meilisearch 本体イメージで再デプロイ
  - 併せて `MEILI_HTTP_ADDR=0.0.0.0:8080` を設定（Cloud Run ポート待受け要件）
- `meili-search` は `allUsers` invoke 可に変更し、IAMトークン必須を解除
- `search-api` に `MEILI_BASE_URL` を設定し、`MEILI_REQUIRE_IDENTITY_TOKEN=false` に変更
- `ml/data/loaders/meili_sync.py`
  - IDトークン取得フォールバック追加（ADC失敗時に `gcloud auth print-identity-token`）
  - Meilisearch `202` タスクの完了待ちを追加（`/tasks/{taskUid}` ポーリング）
- `scripts/dev/seed_minimal.py`
  - `MEILI_REQUIRE_IDENTITY_TOKEN` 環境変数で `--require-identity-token` 付与を切替可能化（既定 false）
- スモーク用クエリを seed データに整合
  - `scripts/local/search_check.py`
  - `scripts/local/feedback_check.py`
  - `scripts/local/training_label_seed.py`

### 3) 検証結果

- `make seed-test` : **PASS**
  - `properties_cleaned=5 / features_today=5 / embeddings=5`
  - `synced_documents=5`（Meilisearchタスク完了待ち込み）
- `make ops-search` : **PASS**
- `make ops-feedback` : **PASS**
- `make ops-ranking` : **PASS**
- `make ops-label-seed` : **PASS**（`posted=3`）
- `make ops-check-retrain` : **PASS**
- `make ops-daily` : **PASS**
- `make run-all` : **PASS**（一連フロー完走）

### 4) 現在の状態

- Phase 4 `study-hybrid-search-cloud` の個別調整方針で、GCP上ハイブリッド検索の動作確認は完了
- ただし運用面では `search_volume.sql` が `n=0` のため、継続的な実トラフィック監視は別途必要
