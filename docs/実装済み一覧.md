# 実装済み一覧

最終更新: 2026-03-26

---

## ディレクトリ構成

```
study-gcp-mlops/
├── .github/workflows/      # CI/CD（GitHub Actions）
├── docs/                   # ドキュメント
├── makefiles/              # Makefile分割モジュール
├── scripts/                # 自動化スクリプト
├── src/
│   ├── api/                # Cloud Run Service（将来）
│   │   ├── predict/        # 推論エンドポイント
│   │   └── service/        # APIサービス
│   └── batch/              # Cloud Run Job（ML学習パイプライン）
└── terraform/              # GCPインフラ定義
```

---

## ファイル一覧

### ルート

| ファイル | 説明 |
|---------|------|
| `.env.example` | 環境変数テンプレート（GCS・ML・Discord） |
| `.gitignore` | 除外設定（credentials, tfstate, mlruns等） |
| `CLAUDE.md` | Claude Code向けプロジェクト説明書 |
| `Makefile` | ビルド・デプロイコマンド統合エントリポイント |
| `README.md` | プロジェクト概要・技術スタック・構成説明 |

### .github/workflows/

| ファイル | 説明 |
|---------|------|
| `batch-deploy.yml` | batch CI/CD（test→build→push→Cloud Run Job更新） |

### docs/

| ファイル | 説明 |
|---------|------|
| `Cloud Scheduler.md` | Cloud Schedulerのcron定期実行・Discord通知設計 |
| `CloudRunJob_ハローワールド手順.md` | Cloud Run Jobの初期セットアップ手順 |
| `GCP_CLIセットアップ手順.md` | Google Cloud CLIインストール手順 |
| `GCP_CloudRun_Terraform手順.md` | Terraform経由のGCP・CloudRun構築手順 |
| `GitHub Actions.md` | GitHub Actions CI/CD設計方針 |
| `ml.md` | モデル選定理由（RandomForest）と実装方針 |
| `仕様、設計書.md` | アーキテクチャ・Terraform設計・デプロイフロー詳細 |
| `実装済み一覧.md` | 本ファイル |

### makefiles/

| ファイル | 説明 |
|---------|------|
| `api.mk` | FastAPI APIのビルド・デプロイコマンド（将来） |
| `batch.mk` | ML学習パイプラインのテスト・ビルド・デプロイ・MLflow UI |
| `gcp.mk` | GCP API有効化・SA権限・Docker認証設定 |
| `terraform.mk` | Terraform init/plan/apply/destroy/fmt |

### scripts/

| ファイル | 説明 |
|---------|------|
| `deploy_batch.py` | batch冪等デプロイ（repo→build→push→apply→run） |
| `monitor_batch.py` | Cloud Run Job実行結果監視＋Discord失敗通知 |
| `reset.py` | GCP全リソース削除＆ローカル・ML成果物クリーン |
| `setup-gcp.sh` | Google Cloud CLIインストール・初期設定 |
| `setup-terraform.sh` | Terraformインストール・初期設定 |

### src/api/predict/

| ファイル | 説明 |
|---------|------|
| `main.py` | FastAPI推論エンドポイント（ハローワールド） |
| `requirements.txt` | 依存ライブラリ（FastAPI・uvicorn） |

### src/api/service/

| ファイル | 説明 |
|---------|------|
| `main.py` | FastAPI推論サービス（ハローワールド） |
| `requirements.txt` | 依存ライブラリ（FastAPI・uvicorn） |

### src/batch/（ML学習パイプライン）

| ファイル | 説明 |
|---------|------|
| `Dockerfile` | Python 3.11-slim＋データ事前キャッシュ コンテナ定義 |
| `dataset.py` | California Housing取得＆train/test分割 |
| `train.py` | RandomForest構築・学習・評価（RMSE・MAE） |
| `model_store.py` | モデルのローカル/GCS保存・読み込み |
| `main.py` | パイプライン（データ→学習→評価→MLflow→GCS保存→ログ出力） |
| `requirements.txt` | 依存（scikit-learn・pandas・gcs・mlflow） |
| `requirements-dev.txt` | 開発依存（pytest） |
| `test_main.py` | データ・学習・保存・ログ・MLflow統合テスト（11件） |

### terraform/

| ファイル | 説明 |
|---------|------|
| `artifact_registry.tf` | Docker形式Artifact Registryリポジトリ定義 |
| `cloud_run_job.tf` | Cloud Run Job（ml-batch）定義＋環境変数 |
| `cloud_scheduler.tf` | Cloud Scheduler cron定期実行（毎日9:00 JST） |
| `gcs.tf` | GCSデータバケット定義（uniform access） |
| `iam.tf` | GCS書き込み・Scheduler実行権限付与 |
| `provider.tf` | Google provider v5・credentials認証設定 |
| `variables.tf` | プロジェクト・リージョン・リポジトリ・バケット名 |
